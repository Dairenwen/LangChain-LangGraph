5. 使用 LangSmith 跟踪 LLM 应用
使用 LangChain 构建的许多应用程序，可能会包含多个步骤和多次的 LLM 调用。随着这些应用程序变
得越来越复杂，作为开发者，我们能够检查链或代理内部到底发生了什么变得至关重要。最好的方法
是使用 LangSmith。
LangSmith 与框架无关，它可以与 langchain 和 langgraph 一起使用，也可以不使用。LangSmith 是
一个用于帮助我们构建生产级 LLM 应用程序的平台，它将密切监控和评估我们的应用。
LangSmith 平台地址：https://smith.langchain.com/  （新用戶需要注册）
要想让 LangSmith 跟踪 LLM 应用，第一步申请 LangSmith API Key，点击 Settings，就会跳转
到"API Keys"设置页面，若没有跳转，可以在左侧 tab 栏中找到进入。

创建完成后，保存好你的 API Key。
接下来配置两个环境变量：
代码块
1 LANGSMITH_TRACING="true"
2 LANGSMITH_API_KEY="你的 LangSmith API Key"

配置完成后，让我们任意执行代码：
代码块
1 from langchain_openai import ChatOpenAI
2 from pydantic import BaseModel, Field
3 from langchain_core.tools import tool
4 from langchain_core.messages import HumanMessage
5
6 # 定义大模型
7 model = ChatOpenAI(model="gpt-4o-mini")
8
9 # 结构输出对象
10 class SearchResult(BaseModel):
11 """结构化搜索结果。"""
12
13 query: str = Field(description="搜索查询")
14 findings: str = Field(description="调查结果摘要")
15
16 @tool
17 def web_search(query: str) -> str:
18 """在网上搜索信息。
19 Args:
20 query: 搜索查询
21 """
22 return "西安今天多云转小雨，气温18-23度，东南风2级，空气质量良好"
23
24 # 手动将工具结果加入消息列表
25 model_with_search = model.bind_tools([web_search])
26 messages = [
27 HumanMessage("搜索当前最新的西安的天气")
28 ]
29 ai_msg = model_with_search.invoke(messages)
30 messages.append(ai_msg)
31 for tool_call in ai_msg.tool_calls:
32 tool_msg = web_search.invoke(tool_call)
33 messages.append(tool_msg)
34
35 structured_search_model =
model_with_search.with_structured_output(SearchResult)
36 result = structured_search_model.invoke(messages)
37 print(result)
查看 LangSmith 平台，这将在 LangSmith 的默认跟踪项目中生成调用的跟踪。点击最新一次的调用
追踪：

跟踪会以瀑布流形式展示调用的完整步骤，以及每个步骤的详细信息和耗时。让我们能够检查内部到
底发生了什么！！
解释：
• ：可运行序列，就是我们之前讲过的 链 ，即我们将
RunnableSequence
model_with_search.invoke() 的结果（构造成 ），当作入参传递给
ToolMessage
structured_search_model.invoke()。
◦ 说明：本次调用是RunnableSequence。但不是每次都展示RunnableSequence，根据实际情
况而定。
• ：实际处理的第一步内容，调用聊天模型，生成结果。
ChatOpenAI

• ：实际处理的第二步内容，表示将 python 可调用对象转换为  ，
RunnableLambda Runnable
其实就是将 AI 生成的结果转换成为结构化对象。
可以看到我们在使用 LangSmith 时没有代码介入，只需要配置下环境，就可以直接监控我们的应用。

三、核心组件（Components）
前置说明，从这一小节开始，我们将会学习大量的概念、术语，及其具体用法和对应的接口方法。
但不用担心，它们本质上都是围绕大模型调用衍生出来额的一系列功能扩展，因为大模型也不是随随
便便就能被很好的调用起来。因此，为了让整个交互流程更规范、更高效，LangChain 定义了一系列
的组件使我们与大模型的交互更规范。
因此在学习过程中，有几点要提前强调：
1. 理解每个组件的作用和适用场景是最重要的，不需要死记硬背接口和参数；
2. 因为课程中会逐步教大家如何查阅官方文档，掌握自主查询的方法才是关键；
3. 但前期我们仍会详细讲解常用接口和参数，随着内容深入，将更侧重核心功能与查询思路的培养。
重要的内容我们都会覆盖到，大家可以放心。
1. 消息（Messages）
消息是聊天模型中的通信单位，用于表示聊天模型的 输入 和 输出 ，以及可能与对话关联的任何其
他 上下文或元数据 。
1.1 LLM 消息结构
每条消息都有一个角色和内容，以及因 LLM 的不同而不同的附加元数据。
• 消息角色 (Role)：用来区分对话中不同类型的消息，并帮助聊天模型了解如何响应给定的消息序
列。
角色  描述
system（系统角色）  用于告诉聊天模型如何行为并提供额外的上下文。并非所有聊天模型提供
商都支持。
user（用戶角色）  表示用戶与模型交互的输入，通常以文本或其他交互式输入的形式。
assistant（助理角色）  表示来自模型的响应，其中可以包括文本或调用工具的请求。
tool（工具角色）  用于在检索外部数据或将工具调用的结果传递回模型的消息。与支持工具
调用的聊天模型一起使用。

• 消息内容 (Content)：表示多模态数据 （例如，图像、音频、视频）的消息文本或字典列表的内
容。内容的具体格式可能因底层不同的 LLM 而异。目前，大多数模型都支持文本作为主要内容类
型，对多模态数据的支持仍然有限。
• 消息其他元数据 (Additional metadata)
元数据  描述
ID  消息标识符。
Name  名称允许区分具有相同角色的不同实体。并非所有型号都支持此功能！
Metadata 有关消息的其他信息，例如时间戳、令牌使用情况等。
Tool Calls  模型发出的一个或多个工具的调用请求
下面展示一个 OpenAI 的格式消息列表：
代码块
1 [
2 {
3 "role": "user",
4 "content": "Hello, how are you?",
5 },
6 {
7 "role": "assistant",
8 "content": "I'm doing well, thank you for asking.",
9 },
10 {
11 "role": "user",
12 "content": "Can you tell me a joke?",
13 }
14 ]
LangChain 接受下面的格式作为聊天模型的输入 ：
代码块
1 chat_model.invoke([
2 {
3 "role": "user",
4 "content": "Hello, how are you?",
5 },
6 {
7 "role": "assistant",

8 "content": "I'm doing well, thank you for asking.",
9 },
10 {
11 "role": "user",
12 "content": "Can you tell me a joke?",
13 }
14 ])
1.2 LangChain 消息
LangChain 提供了一种统一的消息格式，可以跨聊天模型使用，允许用戶使用不同的聊天模型，而无
需担心每个模型提供商使用的消息格式的具体细节。例如：
代码块
1 openai_model = init_chat_model("gpt-4o-mini", model_provider="openai")
2
3 anthropic_model = init_chat_model("claude-3-5-sonnet-latest",
model_provider="anthropic")
4
5 deepseek_model = init_chat_model("deepseek-chat", model_provider="deepseek")
6
7 google_genai_model = init_chat_model("gemini-2.5-flash",
model_provider="google_genai")
8
9 model = init_chat_model(...)
这些模型提供商不同，但对于其输入和输出，统一使用 LangChain 的消息格式。LangChain 消息格式
主要分为五种，分别是：
消息类型  对应角色  描述
SystemMessage  对应 system 系统角色  用于启动 AI 模型的行为并提供额外的上下文，例如指示
模型采用特定角色或设定对话的基调（例如，“你是一
个后端开发的专家”）。
HumanMessage  对应 user 用戶角色  人类消息表示用戶与模型交互的输入。
大多数聊天模型都希望用戶输入采用文本形式。
AIMessage  对应 assistant 助理角色  这是来自模型的响应，其中可以包括文本或调用工具的
请求。它还可能包括其他媒体类型，如图像、音频或视
频⸺尽管这目前仍然不常见。

AIMessageChunk  对应 assistant 助理角色， 通常在生成聊天模型时流式传输响应，因此用戶可以实
用于流式响应  时看到响应，而不是等待生成整个响应后再显示。
ToolMessage  对应 tool 工具角色  这表示一条角色为“tool”的消息，其中包含调用工具
的结果。
这几个消息类型，我们已经全部见过！它们都是 LangChain   的子类，全部是作为
BaseMessage
LangChain 聊天模型的输入和输出！！
1.2.1 BaseMessage 抽象消息类
是作为 LangChain 聊天模型的输
class langchain_core.messages.base.BaseMessage
入和输出！！
参数如下：
• ：消息的字符串内容。
content
• ：与消息关联的其他有效负载数据。对于来自 AI 的消息，可能包括模
additional_kwargs
型提供程序编码的工具调用。
• ：响应元数据。例如：响应标头、logprobs、令牌计数、模型名称。
response_metadata
• ：消息的类型。必须是消息类型唯一的字符串。此字段的目的是在对消息进行反序列化时
type
方便地识别消息类型。
• ：消息名称，为消息提供一个人类可读的名称。该字段的使用是可选的，是否使用它取决
name
于模型实现。
• ：消息的可选唯一标识符。理想情况下，这应该由创建消息的提供者/模型提供。
id
内置方法：
pretty_print() → None ：打印消息的漂亮表示。
pretty_repr(html: bool = False) → str ：获得消息的漂亮表示。
◦ 请求：是否将消息格式化为 HTML。如果为 True，则消息将使用 HTML 标记进行格式化。默
认值为 False。
◦ 响应：这是消息的漂亮表示。
text() → str ：获取消息的文本内容。
1.2.2 对话模式
大多数对话都以设置对话上下文的系统消息开始。接下来是包含用戶输入的用戶消息，然后是包含模
型响应的助手消息。如下图所示：

1.3 缓存历史消息
1.3.1 多轮对话
在与大型语言模型交互的过程中，我们常常体验到与智能助手进行连贯多轮对话的便利性。但目前我
们的系统还不支持此功能，代码如下：
代码块
1 from langchain_openai import ChatOpenAI
2 from langchain_core.messages import HumanMessage
3
4 # 定义大模型
5 model = ChatOpenAI(model="gpt-4o-mini")
6
7 # 第一次对话
8 result = model.invoke([HumanMessage(content="Hi! I'm Bob")])
9 result.pretty_print()
10 # 第二次对话
11 result = model.invoke([HumanMessage(content="What's my name?")])
12 result.pretty_print()
打印结果：
代码块
1 ================================== Ai Message
==================================
2
3 Hi Bob! How can I assist you today?
4 ================================== Ai Message
==================================
5

6 I'm sorry, but I don't have access to personal information about you unless
you share it with me. What would you like me to call you?
可以发现，聊天模型并不认识我们，更别说支持更多轮的对话了~
稍作修改，让我们将 AI 回复给我们的响应跟着新的用戶消息一起发给聊天模型试试。代码如下：
代码块
1 from langchain_openai import ChatOpenAI
2 from langchain_core.messages import HumanMessage, AIMessage
3
4 # 定义大模型
5 model = ChatOpenAI(model="gpt-4o-mini")
6 # 记录消息
7 messages = [
8 HumanMessage(content="Hi! I'm Bob"),
9 AIMessage(content="Hello Bob! How can I assist you today?"),
10 HumanMessage(content="What's my name?"),
11 ]
12 model.invoke(messages).pretty_print()
打印结果：
代码块
1 ================================== Ai Message
==================================
2
3 Your name is Bob! How can I help you today, Bob?
从结果可知，只要将历史消息，重新发送给聊天模型，那么就可以实现多轮对话的功能。
1.3.2 内存缓存
那么对于历史消息的管理就显得尤为重要。在 LangChain 老版本中，可以使用
消息历史类来包装另一个 Runnable 并为其管理聊天消息历史
RunnableWithMessageHistory
记录。它将跟踪模型的输入和输出，并将其存储在某个数据存储中。未来的交互将加载这些消息，并
将其作为输入的一部分传递给链。
代码如下：
代码块

1 from langchain_openai import ChatOpenAI
2 from langchain_core.messages import HumanMessage, AIMessage
3 from langchain_core.chat_history import BaseChatMessageHistory,
InMemoryChatMessageHistory
4 from langchain_core.runnables.history import RunnableWithMessageHistory
5
6 # 定义大模型
7 model = ChatOpenAI(model="gpt-4o-mini")
8
9 store = {}
10 # 接受一个 session_id 并返回一个消息历史对象。
11 # 这个 session_id 用于区分不同的对话，并应作为配置的一部分在调用新链时传入
12 def get_session_history(session_id: str) -> BaseChatMessageHistory:
13 if session_id not in store:
14 # InMemoryChatMessageHistory() 将消息存储在内存列表中。
15 store[session_id] = InMemoryChatMessageHistory()
16 return store[session_id]
17
18 # 包装model，管理聊天消息历史记录
19 with_message_history = RunnableWithMessageHistory(model, get_session_history)
20
21 config = {"configurable": {"session_id": "1"}}
22
23 with_message_history.invoke(
24 [HumanMessage(content="Hi! I'm Bob")],
25 config=config,
26 ).pretty_print()
27
28 with_message_history.invoke(
29 [HumanMessage(content="What's my name?")],
30 config=config,
31 ).pretty_print()
 类初始
class langchain_core.runnables.history.RunnableWithMessageHistory
化参数说明：
• ：被包装 Runnable 实例，这里就是我们定义的聊天模型
runnable
• ：返回类型为 的函数，传入后作为回
get_session_history BaseChatMessageHistory
调函数。此函数接受一个 字符串类型，并返回相应的聊天消息历史记录实例。
session_id
 类方法
class langchain_core.runnables.history.RunnableWithMessageHistory
说明：
• 方法：此方法与其他 Runnable 实例的   方法相同。只不过注意其
.invoke() .invoke()
config配置，需要配置成 ，让
config={"configurable": {"session_id": ""}}
可以读取到会话id。
RunnableWithMessageHistory

最终打印结果：
代码块
1 ================================== Ai Message
==================================
2
3 Hi Bob! How can I assist you today?
4 ================================== Ai Message
==================================
5
6 Your name is Bob! How can I help you today?
记忆功能已经实现！
1.3.2.1 说明
从 LangChain 的 v0.3 版本开始，官方建议 LangChain 用戶不要使用
，而是利用 持久性 来完成（见 LangGraph 章
RunnableWithMessageHistory LangGraph
节）。
原因是它们的功能有限，不太适合现实世界的对话式 AI 应用程序。这些内存抽象缺乏对多用戶、多对
话场景的内置支持，而这对于实际的对话式人工智能系统至关重要。这些实现中的大多数已在
LangChain 0.3.x 中被正式弃用，取而代之的是  持久性 。 持久性 非常灵
LangGraph LangGraph
活，可以支持比   接口更广泛的用例。我们会在 篇
RunnableWithMessageHistory LangGraph
章中学习它！
因此，  这部分我们讲解的并不深入。在之前，对于生产环境，
RunnableWithMessageHistory
我们还需要使用聊天消息历史记录的持久化实现，例如  （） ，而不
RedisChatMessageHistory
是 ，但现在也已不推荐新应用使用它们了。
InMemoryChatMessageHistory()
1.4 管理历史消息
1.4.1 前置概念
1.4.1.1 上下文窗口
管理历史消息，无非就是理解如何“管理”，“管理”无非也就是一些 “CRUD”。那么在了解如何
管理消息之前，需要先了解下多轮对话的核心概念：上下文窗口。上下文窗口可以理解为模型的“短
期工作记忆区”，即 LLM 在一次处理请求时，所能查看和处理的最大 Token 数量，它包含了：
• 用戶的输入
• 大模型的输出

• 有时还包括系统指令（SystemMessage）和对话历史。
不同大模型支持的上下文窗口大小不同，例如：
• OpenAI 下 GPT-5 模型上下文窗口为 400000 （最大 Token 数量）
• GPT-4.1 模型上下文窗口为 1047576 （最大 Token 数量）
• 其他模型上下文窗口可参考对应模型官网说明，如 OpenAI 下模型可以参考这里。
1.4.1.2 Token
在自然语言处理（NLP）中，Token 是文本的基本单位。它不是完全等同于一个单词或一个汉字，而
是一个更细粒度的划分。
为什么用 Token？计算机无法直接理解文字，它需要将文本转换为数字（向量）。Tokenization（令
牌化）就是这个转换过程的第一步，将句子分解成模型可以理解和处理的碎片。
• 对于英文：  个 个字符或 个单词 ，1000 个 Tokens 约等于 750 个英文单词。
1 Token ~= 4 0.75
一个 Token 可以是一个单词（如  ）、一个词根（如   在   中），
"apple" "un" "unlikely"
或者一个标点符号（如  ）。例如，  可能会被分成
"." "ChatGPT is great!" ["Chat",
这 6 个 Token。
"G", "PT", " is", " great", "!"]
• 对于中文：  个汉字 个 ，1000 个 Tokens 大约相当于 500-700 个汉字。常见
1 ~= 1.5-2 Tokens
的词和字可能是一个 Token，生僻字或复杂词可能会被拆分成多个。
举个例子， 上下文窗口就像一个固定大小的工作台，再把 Token 比作一个积木零件，把大模型比作一
个工匠。工匠需要拼出模型，必须把所需的零件（输入的 Token） 放在工作台上，一边拼装（生成回
复），一边把拼好的部分（输出的 Token）也放在工作台上。整个过程（输入+输出）中，工作台上的
所有积木（Tokens）总数都不能超过工作台的最大容量（上下文窗口大小）。

![Tokenization 工作台示意图](assets/diagrams/langchain-tokenization-workbench.png)

如果最初的零件太多，占满了工作台，工匠就没有空间进行拼装了。这时你就需要减少零件（精简输
入）。
1.4.2 消息裁剪
有了上下文窗口和 Token 的认知，再来看多轮对话的实现原理，其实就是：
• 输入 = 系统消息 + 对话历史 + 最新用戶问题
• 对于模型来说，并不真正“记忆”，而是每次都将完整的上下文重新输入。
由于所有模型的上下文窗口大小都是有限的，这意味着作为输入的 Token 也是有限的。如果有累积了
很长的消息历史记录，则需要管理传递给模型的消息的长度。
可用于将聊天历史记录的大小减小为指定的令牌计数或指定的消息计数。
trim_messages
1.4.2.1 基于输入 Token 数的修剪
下面演示一个通过 裁剪消息的示例（基于输入 Token 数的修剪）。
trim_messages
先来看看不做任何输入限制的聊天：
代码块
1 from langchain_openai import ChatOpenAI
2 from langchain_core.messages import HumanMessage, SystemMessage, AIMessage,
trim_messages
3
4 # 定义大模型
5 model = ChatOpenAI(model="gpt-4o-mini")
6 # 历史消息记录

7 messages = [
8 SystemMessage(content="you're a good assistant"),
9 HumanMessage(content="hi! I'm bob"),
10 AIMessage(content="hi!"),
11 HumanMessage(content="I like vanilla ice cream"),
12 AIMessage(content="nice"),
13 HumanMessage(content="whats 2 + 2"),
14 AIMessage(content="4"),
15 HumanMessage(content="thanks"),
16 AIMessage(content="no problem!"),
17 HumanMessage(content="having fun?"),
18 AIMessage(content="yes!"),
19 HumanMessage(content="What's my name?"),
20 ]
21 print(model.invoke(messages))
打印结果：
代码块
1 content='Your name is Bob.'
2 additional_kwargs={'refusal': None}
3 response_metadata={
4 'token_usage': {
5 'completion_tokens': 5,
6 'prompt_tokens': 88,
7 'total_tokens': 93,
8 'completion_tokens_details': {
9 'accepted_prediction_tokens': 0,
10 'audio_tokens': 0,
11 'reasoning_tokens': 0,
12 'rejected_prediction_tokens': 0
13 },
14 'prompt_tokens_details': {
15 'audio_tokens': 0,
16 'cached_tokens': 0
17 }
18 },
19 'model_name': 'gpt-4o-mini-2024-07-18',
20 'system_fingerprint': 'fp_51db84afab',
21 'id': 'chatcmpl-C8KeAy4qX9DaILGRYV6I3v6iOVSjM',
22 'service_tier': 'default',
23 'finish_reason': 'stop',
24 'logprobs': None
25 }
26 id='run--6f8eb5dc-69dc-4dbe-8109-d0e99991ff9e-0'

27 usage_metadata={
28 'input_tokens': 88,
29 'output_tokens': 5,
30 'total_tokens': 93,
31 'input_token_details': {
32 'audio': 0,
33 'cache_read': 0
34 },
35 'output_token_details': {
36 'audio': 0,
37 'reasoning': 0
38 }
39 }
40
从打印结果看来，LLM 还认识我们，且共输入了 88 tokens。接下来让我们对消息进行裁剪，我们只希
望将来输入时，最多输入 65 tokens，超出的需要按照一定的“规则”进行裁剪，代码如下：
代码块
1 from langchain_openai import ChatOpenAI
2 from langchain_core.messages import HumanMessage, SystemMessage, AIMessage,
trim_messages
3
4 # 定义大模型
5 model = ChatOpenAI(model="gpt-4o-mini")
6
7 # 历史消息记录
8 messages = [
9 SystemMessage(content="you're a good assistant"),
10 HumanMessage(content="hi! I'm bob"),
11 AIMessage(content="hi!"),
12 HumanMessage(content="I like vanilla ice cream"),
13 AIMessage(content="nice"),
14 HumanMessage(content="whats 2 + 2"),
15 AIMessage(content="4"),
16 HumanMessage(content="thanks"),
17 AIMessage(content="no problem!"),
18 HumanMessage(content="having fun?"),
19 AIMessage(content="yes!"),
20 HumanMessage(content="What's my name?"),
21 ]
22
23
24 # 使用 trim_messages 减少发送给模型的消息数量
25 trimmer = trim_messages(

26 max_tokens=65, # 修剪消息的最大令牌数，根据你想要的谈话长度来调整
27 strategy="last", # 修剪策略：
28 # “last”（默认）：保留最后的消息。
29 # “first”：保留最早的消息。
30 token_counter=model, # 传入一个函数或一个语言模型（因为语言模型有消息令牌计数方
法）
31 include_system=True, # 如果想始终保留初始系统消息，可以指定
include_system=True
32 allow_partial=False, # 是否允许拆分消息的内容
33 start_on="human", # 如果需要确保我们的第一条消息（不包括系统消息）始终是特定
类型，可以指定 start_on
34 )
35
36 chain = trimmer | model
37 print(chain.invoke(messages))
打印结果：
代码块
1 content='I don’t know your name. Would you like to share it with me?'
2 additional_kwargs={'refusal': None}
3 response_metadata={
4 'token_usage': {
5 'completion_tokens': 16,
6 'prompt_tokens': 60,
7 'total_tokens': 76,
8 'completion_tokens_details': {
9 'accepted_prediction_tokens': 0,
10 'audio_tokens': 0,
11 'reasoning_tokens': 0,
12 'rejected_prediction_tokens': 0
13 },
14 'prompt_tokens_details': {
15 'audio_tokens': 0,
16 'cached_tokens': 0
17 }
18 },
19 'model_name': 'gpt-4o-mini-2024-07-18',
20 'system_fingerprint': 'fp_560af6e559',
21 'id': 'chatcmpl-C8KoOHEnixgpyZaPrvwKecemxjniM',
22 'service_tier': 'default',
23 'finish_reason': 'stop',
24 'logprobs': None
25 }
26 id='run--e0c0a07b-54b5-4910-bbb5-4a725a633c77-0'

27 usage_metadata={
28 'input_tokens': 60,
29 'output_tokens': 16,
30 'total_tokens': 76,
31 'input_token_details': {
32 'audio': 0, 'cache_read': 0
33 },
34 'output_token_details': {
35 'audio': 0,
36 'reasoning': 0
37 }
38 }
可以看见，此时我们的输入 message 已经被修剪了。被修剪了哪些消息呢？来看下：
代码块
1 trimmer = trim_messages(
2 ...
3 )
4 # 打印经过裁剪器裁剪后的结果
5 print(trimmer.invoke(messages))
结果如下：
代码块
1 [
2 SystemMessage(content="you're a good assistant", additional_kwargs={},
response_metadata={}),
3 HumanMessage(content='whats 2 + 2', additional_kwargs={},
response_metadata={}),
4 AIMessage(content='4', additional_kwargs={}, response_metadata={}),
5 HumanMessage(content='thanks', additional_kwargs={}, response_metadata=
{}),
6 AIMessage(content='no problem!', additional_kwargs={}, response_metadata=
{}),
7 HumanMessage(content='having fun?', additional_kwargs={},
response_metadata={}),
8 AIMessage(content='yes!', additional_kwargs={}, response_metadata={}),
9 HumanMessage(content="What's my name?", additional_kwargs={},
response_metadata={})
10 ]

从结果来看，确实是按照我们给定的裁剪“规则”来完成的。修剪聊天记录后，生成的聊天记录（输
入）应该有效，需遵循对话模式原则：
• 聊天记录以 或 开头，后跟 。这可以通过
HumanMessage SystemMessage HumanMessage
设置 来实现。
start_on="human"
• 聊天记录以 或 结尾。这可以通过设置
HumanMessage ToolMessage ends_on=
来实现。
("human", "tool")
• 只能出现在涉及工具调用的 之后。
ToolMessage AIMessage
• 如果原始聊天历史记录中存在 ，则新聊天历史记录应包括
SystemMessage
，因为  包含对聊天模型的特殊说明。
SystemMessage SystemMessage SystemMessage
总是历史记录中的第一条消息（如果存在）。这可以通过设置 。
include_system=True
1.4.2.2 基于消息数的修剪
除了基于 token 的修剪，还可以通过设置   根据消息数修剪聊天记录。在这
token_counter=len
种情况下，  将控制最大消息数。示例如下：
max_tokens
代码块
1 from langchain_openai import ChatOpenAI
2 from langchain_core.messages import HumanMessage, SystemMessage, AIMessage,
trim_messages
3
4 # 定义大模型
5 model = ChatOpenAI(model="gpt-4o-mini")
6
7 # 历史消息记录
8 messages = [
9 SystemMessage(content="you're a good assistant"),
10 HumanMessage(content="hi! I'm bob"),
11 AIMessage(content="hi!"),

12 HumanMessage(content="I like vanilla ice cream"),
13 AIMessage(content="nice"),
14 HumanMessage(content="whats 2 + 2"),
15 AIMessage(content="4"),
16 HumanMessage(content="thanks"),
17 AIMessage(content="no problem!"),
18 HumanMessage(content="having fun?"),
19 AIMessage(content="yes!"),
20 HumanMessage(content="What's my name?"),
21 ]
22
23 # 使用 trim_messages 减少发送给模型的消息数量
24 trimmer = trim_messages(
25 max_tokens=11, # 最大消息数
26 strategy="last", # 修剪策略：
27 # “last”（默认）：保留最后的消息。可获取消息列表中的最后
一个 max_tokens
28 # “first”：保留最早的消息。
29 token_counter=len, # 根据消息数裁剪
30 include_system=True, # 如果想始终保留初始系统消息，可以指定
include_system=True
31 allow_partial=False, # 是否允许拆分消息的内容
32 start_on="human", # 如果需要确保我们的第一条消息（不包括系统消息）始终是特定
类型，可以指定 start_on
33 )
34
35 print(trimmer.invoke(messages))
结果如下：
代码块
1 [
2 SystemMessage(content="you're a good assistant", additional_kwargs={},
response_metadata={}),
3 HumanMessage(content='I like vanilla ice cream', additional_kwargs={},
response_metadata={}),
4 AIMessage(content='nice', additional_kwargs={}, response_metadata={}),
5 HumanMessage(content='whats 2 + 2', additional_kwargs={},
response_metadata={}),
6 AIMessage(content='4', additional_kwargs={}, response_metadata={}),
7 HumanMessage(content='thanks', additional_kwargs={}, response_metadata=
{}),
8 AIMessage(content='no problem!', additional_kwargs={}, response_metadata=
{}),

9 HumanMessage(content='having fun?', additional_kwargs={},
response_metadata={}),
10 AIMessage(content='yes!', additional_kwargs={}, response_metadata={}),
11 HumanMessage(content="What's my name?", additional_kwargs={},
response_metadata={})
12 ]
1.4.3 消息过滤
在更复杂的场景下，我们可能会使用消息列表来跟踪状态，例如我们可能只想将这个完整消息列表的
子集传递模型调用，而不是所有的历史记录。
方法则可以轻松地按类型、ID 或名称过滤 message。
filter_messages
下面演示相关过滤示例，首先准备消息列表：
代码块
1 from langchain_core.messages import HumanMessage, SystemMessage, AIMessage,
filter_messages
2
3 # 历史消息记录
4 messages = [
5 SystemMessage("你是一个聊天助手", id="1"),
6 HumanMessage("示例输入", id="2"),
7 AIMessage("示例输出", id="3"),
8 HumanMessage("真实输入", id="4"),
9 AIMessage("真实输出", id="5"),
10 ]
• 按类型进行筛选：
代码块
1 print(filter_messages(messages, include_types="human"))
2
3 # 注意写法等价于：
4 # print(filter_messages(include_types="human").invoke(messages))
5
6 # 结果如下：
7 # [
8 # HumanMessage(content='示例输入', additional_kwargs={}, response_metadata=
{}, name='example_user', id='2'),
9 # HumanMessage(content='真实输入', additional_kwargs={}, response_metadata=
{}, name='bob', id='4')

10 # ]
• 按类型+ID进行筛选：
代码块
1 print(filter_messages(messages, include_types=[HumanMessage, AIMessage],
exclude_ids=["3"]))
2
3 # 结果如下：
4 # [
5 # HumanMessage(content='示例输入', additional_kwargs={}, response_metadata=
{}, name='example_user', id='2'),
6 # HumanMessage(content='真实输入', additional_kwargs={}, response_metadata=
{}, name='bob', id='4'),
7 # AIMessage(content='真实输出', additional_kwargs={}, response_metadata={},
name='alice', id='5')
8 # ]
1.4.4 消息合并
若我们的消息列表存在连续某种类型相同的消息，但实际上某些模型不支持传递相同类型的连续消
息。因此对于这种情况，我们可以使用 方法轻松合并相同类型的连续消
merge_message_runs
息。
示例如下：
代码块
1 from langchain_openai import ChatOpenAI
2 from langchain_core.messages import HumanMessage, SystemMessage, AIMessage,
merge_message_runs
3
4 # 定义大模型
5 model = ChatOpenAI(model="gpt-4o-mini")
6
7 # 历史消息记录
8 messages = [
9 SystemMessage("你是一个聊天助手。"),
10 SystemMessage("你总是以笑话回应。"),
11 HumanMessage("为什么要使用 LangChain?"),
12 HumanMessage("为什么要使用 LangGraph?"),
13 AIMessage("因为当你试图让你的代码更有条理时，LangGraph 会让你感到“节点”是个好主
意！"),

14 AIMessage("不过别担心，它不会“分散”你的注意力！"),
15 HumanMessage("选择LangChain还是LangGraph?"),
16 ]
17
18 merged = merge_message_runs(messages)
19 # 打印合并后的每个消息
20 print("\n".join([repr(x) for x in merged]))
合并结果：
代码块
1 SystemMessage(content='你是一个聊天助手。\n你总是以笑话回应。', additional_kwargs=
{}, response_metadata={})
2 HumanMessage(content='为什么要使用 LangChain?\n为什么要使用 LangGraph?',
additional_kwargs={}, response_metadata={})
3 AIMessage(content='因为当你试图让你的代码更有条理时，LangGraph 会让你感到“节点”是个好
主意！\n不过别担心，它不会“分散”你的注意力！', additional_kwargs={},
response_metadata={})
4 HumanMessage(content='选择LangChain还是LangGraph?', additional_kwargs={},
response_metadata={})
调用大模型：
代码块
1 ## 方式一：
2 merged = merge_message_runs(messages)
3 model.invoke(messages).pretty_print()
4
5 ## 方式二：
6 merger = merge_message_runs()
7 chain = merger | model
8 chain.invoke(messages).pretty_print()
打印结果：
代码块
1 ================================== Ai Message
==================================
2
3 这就像选择汉堡还是热狗！如果你想要一个多层次的体验，选择LangChain；如果你想要一个清晰的链
接，LangGraph就像是你的“支架”！让你的代码加个“料”！

2. 提示词模板（Prompt Template）
2.1 概念
提示词模板（Prompt Template）是 LangChain 的核心抽象之一，它被广泛应用于构建大语言模型
（LLM）应用的各个环节。
简单来说，只要是需要动态、批量、或有结构地向大语言模型【发送请求】的地方，几乎都会用到提
示词模板。
一个简单的例子，假设我们想根据一个城市名询问 LLM 其历史，按照之前的做法，我们可以定义
请介绍上海的历史 、 请介绍西安的历史 消息等等。可
HumanMessage(" ") HumanMessage(" ")
以发现每次询问都会描写重复的消息内容： 请介绍 的历史 。
xxx
在 LangChain 中，针对这种情况，可以定义一个模板：
• 固定文本（模板）： “请介绍{city}的历史。”
• 输入变量： [“city”]
定义好后，可以使用该模板：
• 当我们需要查询北京时，就将 city 变量赋值为 “北京”。模板引擎会生成： “请介绍北京的历
史。”
• 当我们需要查询上海时，就将 city 变量赋值为 “上海”。模板引擎会生成： “请介绍上海的历
史。”
由此可得：提示词模板就是一个可复用的提示词蓝图，它允许我们动态地生成提示词，而不是每次都
手动编写完整的提示词。它类似于编程中的字符串格式化功能。你创建一个带有“占位符”的模板，

然后在运行时，用具体的值（变量）填充这些占位符，从而生成一个最终发送给 LLM 的完整提示词。
提示词模板解决了以下几个核心问题：
1. 可复用性： 只需定义一个模板，就可以用于无数个类似的查询。
2. 关注点分离： 将提示词的结构和逻辑（工程）与具体的内容和数据分离开。提示工程师可以专注于
优化模板，而应用程序则负责提供变量值。
3. 一致性： 确保发送给LLM的提示词结构统一，这有助于获得更稳定、可预测的输出结果。
4. 可维护性： 如果需要修改提示词的风格或结构，只需修改一个模板文件，而不用在代码的无数个地
方进行修改。
2.2 用法
2.2.1 字符串模板
LangChain 提供了   类来轻松实现这一功能。 实现了标准的
PromptTemplate PromptTemplate
Runnable 接口。示例如下：
代码块
1 from langchain_core.prompts import PromptTemplate
2 # 1. 定义模板
3 prompt_template = PromptTemplate.from_template("Translate the following into
{language}")
4 # 2. 实例化模板
5 print(prompt_template.invoke({"language": "Chinese"}))
打印结果：
代码块
1 text='Translate the following into Chinese'
说明：
类，其参数如下：
class langchain_core.prompts.prompt.PromptTemplate
• ：提示模板
template
• ：需要其值作为提示输入的变量的名称列表。
input_variables
内置方法：
：从模板定义提示模板。方法返回了一个 实例
from_template() PromptTemplate

因此除了上面示例中 定义提示模板的方式外，下面这种方法
PromptTemplate.from_template
也可以直接初始化模板：
代码块
1 prompt_template = PromptTemplate(
2 input_variables=["language"],
3 template="Translate the following into {language}",
4 )
2.2.2 聊天消息模板
模板：专为 LangChain 聊天模型设计。可以方便地构建包含
ChatPromptTemplate
、 、  的消息模板。如下代码所示：
SystemMessage HumanMessage AIMessage
代码块
1 from langchain_core.prompts import ChatPromptTemplate
2
3 # 1. 设置模板
4 prompt_template = ChatPromptTemplate(
5 [
6 ("system", "Translate the following into {language}."),
7 ("user", "{text}")
8 ]
9 )
10 # 说明：
11 # 在 0.2.24 版本后可以直接使用ChatPromptTemplate()来初始化模板
12 # 在 0.2.24 版本前，需要使用 ChatPromptTemplate.from_messages()来初始化模板
13
14 # 2. 实例化模板，获取消息实例
15 messagesValue = prompt_template.invoke(
16 {
17 "language": "Chinese",
18 "text": "what is your name?"
19 }
20 )
21 messages = messagesValue.to_messages()
22 print(messages)
打印结果：
代码块

1 [
2 SystemMessage(content='Translate the following into Chinese.',
additional_kwargs={}, response_metadata={}),
3 HumanMessage(content='what is your name?', additional_kwargs={},
response_metadata={})
4 ]
现在，我们可以将该结果发送给任何一个 LLM 来获取答案。如下所示：
代码块
1 from langchain_openai import ChatOpenAI
2 from langchain_core.prompts import ChatPromptTemplate
3 from langchain_core.output_parsers import StrOutputParser
4
5 # 定义大模型
6 model = ChatOpenAI(model="gpt-4o-mini")
7
8 # 1. 设置模板
9 prompt_template = ChatPromptTemplate(
10 [
11 ("system", "Translate the following into {language}."),
12 ("user", "{text}")
13 ]
14 )
15
16 # 2. 实例化模板，获取消息实例
17 messagesValue = prompt_template.invoke(
18 {
19 "language": "Chinese",
20 "text": "what is your name?"
21 }
22 )
23 messages = messagesValue.to_messages()
24 print(messages)
25
26 # 3. 输出解析
27 parser = StrOutputParser()
28 chain = model | parser
29 print(chain.invoke(messages))
打印结果：
代码块
1 你的名字是什么？

由于 同样也实现了标准的 Runnable 接口，因此我们还可以通过链来完成
ChatPromptTemplate
调用。如下所示：
代码块
1 from langchain_openai import ChatOpenAI
2 from langchain_core.prompts import ChatPromptTemplate
3 from langchain_core.output_parsers import StrOutputParser
4
5 # 定义大模型
6 model = ChatOpenAI(model="gpt-4o-mini")
7
8 # 1. 设置模板
9 prompt_template = ChatPromptTemplate(
10 [
11 ("system", "Translate the following into {language}."),
12 ("user", "{text}")
13 ]
14 )
15 # 2. 定义输出解析器
16 parser = StrOutputParser()
17 # 3. 定义链
18 chain = prompt_template | model | parser
19
20 for token in chain.stream(
21 {
22 "language": "English",
23 "text": "你好，我叫斯蒂芬，很高兴认识你"
24 }
25 ):
26 print(token, end="|")
打印结果：
代码块
1 |Hello|,| my| name| is| Stephen|,| nice| to| meet| you|.||
2.2.3 消息占位符

在上面的 ChatPromptTemplate 中，我们看到了如何格式化两条消息，每条消息都是一个字符串。但
如果我们希望将消息插入特定位置怎么办？使用 。
MessagesPlaceholder
负责在特定位置添加消息列表。代码如下：
MessagesPlaceholder
代码块
1 from langchain_core.prompts import ChatPromptTemplate, MessagesPlaceholder
2 from langchain_core.messages import HumanMessage, AIMessage
3
4 prompt_template = ChatPromptTemplate([
5 ("system", "你是一个聊天助手"),
6 MessagesPlaceholder("msgs") # 消息占位符
7 ])
8
9 messages_to_pass = [
10 HumanMessage(content="中国首都是哪里?"),
11 AIMessage(content="中国首都是北京。"),
12 HumanMessage(content="那法国呢?")
13 ]
14
15 formatted_prompt = prompt_template.invoke({"msgs": messages_to_pass})
16 print(formatted_prompt)
打印结果：
代码块
1 messages=[
2 SystemMessage(content='你是一个聊天助手', additional_kwargs={},
response_metadata={}),
3 HumanMessage(content='中国首都是哪里?', additional_kwargs={},
response_metadata={}),
4 AIMessage(content='中国首都是北京。', additional_kwargs={},
response_metadata={}),
5 HumanMessage(content='那法国呢?', additional_kwargs={}, response_metadata=
{})
6 ]
在不显式使用   类也可以完成该能力：
MessagesPlaceholder
代码块
1 from langchain_core.prompts import ChatPromptTemplate
2 from langchain_core.messages import HumanMessage, AIMessage
3
4 prompt_template = ChatPromptTemplate(

5 [
6 ("system", "You are a helpful assistant"),
7 ("placeholder", "{msgs}")
8 ]
9 )
10
11 messages_to_pass = [
12 HumanMessage(content="中国首都是哪里?"),
13 AIMessage(content="中国首都是北京。"),
14 HumanMessage(content="那法国呢?")
15 ]
16
17 formatted_prompt = prompt_template.invoke({"msgs": messages_to_pass})
18 print(formatted_prompt)
2.3 使用 LangChain Hub 的提示词模板
LangChain Hub 是一个用于上传、浏览、拉取和管理提示词(prompts)的地方。
随着 LLM 的发展，提示变得越来越重要。LangChain 正在打造一个与像 GitHub 这样的传统平台，
GitHub长期以来一直是共享和协作代码的首选平台。于是推出了 LangChain Hub 平台。
LangChain Hub 创建一个分享和发现 Prompt 的平台，使得开发者可以更容易地发现新用例和精炼提
示。 这一举措使提示工程师更容易合作，重复使用现有的提示，并对其进行微调以实现特定的结果，
从而加速对话代理和其他基于语言的应用程序的开发和部署。早期的时候 LangChain Hub 有
Prompt、Chain、Agent，现在只有Prompt。
LangChain Hub 官网地址：https://smith.langchain.com/hub/。通过登录到 Hub 来探索所有现有提
示

目前收藏最高的提示词模板是： 。我们就以它为示例，演示一下如
hardkothari/prompt-maker
何使用 LangChain Hub 上的提示。
模板是一个【提示生成器】 ，它可以自动化优化提示的过程，从而提高语言模型在
Prompt Maker
各种应用中的质量和效果。
要想使用该能力，需要先申请并配置 环境变量： 你的
LangSmith LANGSMITH_API_KEY="
。接着，需要从 hub 拉取相应的提示，并使用，代码如下：
LangSmith API Key"
代码块
1 from langchain_openai import ChatOpenAI
2 from langsmith import Client
3
4 # 从 hub 拉取 "hardkothari/prompt-maker" 提示词模板。
5 client = Client()
6 prompt = client.pull_prompt("hardkothari/prompt-maker", include_model=True)
7 # 定义模型
8 model = ChatOpenAI(model="gpt-4o-mini")
9 # 定义链
10 chain = prompt | model
11
12 while True:
13 task = input("\n你的任务是什么？（输入 quit 退出聊天）\n")
14 if task == 'quit':
15 break
16
17 lazy_prompt = input("\n你当前的提示是什么？（输入 quit 退出聊天）\n")
18 if lazy_prompt == 'quit':
19 break
20

21 print("\n Response:")
22 chain.invoke({'lazy_prompt': lazy_prompt, 'task': task}).pretty_print()
运行代码：
代码块
1
2 你的任务是什么？（输入 quit 退出聊天）
3 写一个快排代码
4
5 你当前的提示是什么？（输入 quit 退出聊天）
6 开发专家，需中文回复
7
8 Response:
9 ================================== Ai Message
==================================
10
11 As a coding expert specialized in algorithms and data structures, please write
a detailed implementation of the Quick Sort algorithm in Python.
12
13 ### Instructions:
14 Your code should be well-structured, with clear comments explaining each step
of the algorithm. Additionally, include an example of how to use the function
to sort a list of integers and print the sorted result.
15
16 ### Context:
17 The implementation should focus on efficiency and clarity, ideally spanning no
more than 20 lines of code. Ensure that the prompt is in Chinese, and
structure your response to be easily understandable for readers with a basic
knowledge of programming.
18
19 Example:
20 ```python
21 def quick_sort(arr):
22 # 快速排序函数实现
23 ...
24 return sorted_arr
25
26 # 使用示例
27 unsorted_list = [34, 7, 23, 32, 5, 62]
28 print(quick_sort(unsorted_list))
29 ```
30
31 Please provide the complete code and any additional notes that could aid in
understanding the Quick Sort algorithm. Thank you!

32
33 你的任务是什么？（输入 quit 退出聊天）
34
通过使用这个模板，可以大大减少手动调整提示所需的工作量，从而节省时间和资源。Prompt Maker
通过分析初始提示的结构和内容，然后应用一组预定规则或算法来优化提示，以提高响应质量、清晰
度和相关性。这在提示的质量对模型的输出有很大影响的场景中特别有用，比如客戶服务机器人、对
话代理或数据分析任务。
3. 少样本提示（few-shotting）
3.1 概念
少样本提示是一种通过向 LLM 提供少量具体示例或样本，来教会它如何执行某项特定任务的技术。提
高模型性能的最有效方法之一是给出一个【模型示例】指导大模型你想做什么、怎么做。下面用一个
例子解释少样本提示的作用。
• 参考之前学习的内容，我们可以将其想象为“零样本提示”：直接给模型一道考题，不给任何例
子。模型会凭借已有的知识直接回答。例如我们输入 🦜 ，答案可能是：
"What is 2 9?"
代码块
1 from langchain_openai import ChatOpenAI
2 model = ChatOpenAI(model="gpt-4o-mini")
3 model.invoke("What is 2 🦜 9?").pretty_print()
4
5 # 输出：
6 # ================================== Ai Message
==================================
7 #
8 # 表达式“2🦜9”不是一个标准的数学运算或方程。它似乎是数字2和鹦鹉🦜的组合，后面跟着数字
9。
9 # 它没有特定的数学意义。
• 少样本提示则是在给出考题前，先给它看几道类似的、附有正确答案的例题。添加示例输入和预期
输出的技术给到模型提示，让模型通过例题来理解任务应该怎么做。例如以下样例，模型通过样例
后可以发现 🦜 与 ➕ 含义相似，便可以按照此规则得出 🦜 的结果是 ：
What is 2 9? 11
代码块
1 examples = [
2 {"input": "2 🦜 2", "output": "4"},
3 {"input": "2 🦜 3", "output": "5"},

4 ]
这能解决什么问题？LLM 虽然知识渊博，但有时我们需要它以非常特定的格式、风格或逻辑来回答问
题。提供正确的示例可以减少模型“胡说八道”或犯低级错误的概率，将其输出约束在你提供的范例
范围内。举个例子更能理解：
1. 强制要求模型以特定的格式（如JSON、XML、特定的列表样式）输出结果。样例可以当作格式样
板。
2. 有些任务很难用文字指令清晰描述（例如：“请用莎士比亚的风格写作”）。提供几个例子比写长
篇大论的指令更有效。
3. 对于需要多步推理的复杂任务，示例可以展示出思考链，引导模型遵循类似的推理路径。
3.2 实现少样本提示
实现少样本提示的第一步也是最重要的一步是提出一个好的示例数据集。好的示例应该在运行时相
关、清晰、信息丰富，并提供模型尚不知道的信息。如我们给出的例子，就能很好的提示大模型 🦜
与 ➕ 含义相似：
代码块
1 examples = [
2 {"input": "2 🦜 2", "output": "4"},
3 {"input": "2 🦜 3", "output": "5"},
4 ]
如何让大模型看懂这份示例呢？之前我们说过聊天模型读的是聊天消息。因此，接下来我们需要将示
例集实例化成聊天模型可以读懂的聊天消息。对于 LangChain 就需要创建一个
对象来实例化示例集。
FewShotChatMessagePromptTemplate
是一个提示词模板，专门用来将示例集实例化为聊天消
FewShotChatMessagePromptTemplate
息，用法如下所示：
代码块
1 from langchain_core.prompts import ChatPromptTemplate,
FewShotChatMessagePromptTemplate
2 example_prompt = ChatPromptTemplate(
3 [
4 ("human", "{input}"),
5 ("ai", "{output}"),
6 ]
7 )
8 few_shot_prompt = FewShotChatMessagePromptTemplate(

9 example_prompt=example_prompt, # ChatPromptTemplate，用于格式化单个示例
10 examples=examples, # 样本示例
11 )
12
13 print(few_shot_prompt.invoke({}).to_messages())
可以得到示例集转化的聊天消息列表，结果如下：
代码块
1 [
2 HumanMessage(content='2 🦜 2', additional_kwargs={}, response_metadata=
{}),
3 AIMessage(content='4', additional_kwargs={}, response_metadata={}),
4 HumanMessage(content='2 🦜 3', additional_kwargs={}, response_metadata=
{}),
5 AIMessage(content='5', additional_kwargs={}, response_metadata={})
6 ]
先来看看
class
，它也实
langchain_core.prompts.few_shot.FewShotChatMessagePromptTemplate
现了标准的 Runnable 接口。
类初始化参数说明：
• ：样本示例。
examples
• ：ChatPromptTemplate，用于格式化单个示例
example_prompt
类方法说明：
• 方法：此方法与其他 Runnable 实例的   方法类似。输入一个字典给
.invoke() .invoke()
它，返回完整的提示内容 ：
PromptValue
◦ 的 方法可以将提示值作为【字符串】返回。
PromptValue to_string()
◦ 的 方法可以将提示作为【消息列表】返回。
PromptValue to_messages()
最后，得到示例集消息列表后，就可以带上一起发起请求：
代码块
1 final_prompt = ChatPromptTemplate(
2 [
3 ("system", "你是一个神奇的数学奇才。"),
4 few_shot_prompt,
5 ("human", "{input}"),
6 ]
7 )

8
9 from langchain_openai import ChatOpenAI
10 model = ChatOpenAI(model="gpt-4o-mini")
11 chain = final_prompt | model
12 chain.invoke({"input": "What is 2 🦜 9?"}).pretty_print()
结果如下：
代码块
1 ================================== Ai Message
==================================
2
3 11
计算成功！
3.3 使用案例
3.3.1 案例一：推理引导
我们希望输入：
《教父》和《星球大战》的导演来自同一个国家吗？
让聊天模型可以先分析再得出结论，而不是直接得出结论。分析过程需要展示出来，示例如下：
代码块
1 from langchain_core.prompts import PromptTemplate
2
3 # 创建字符串模板
4 example_prompt = PromptTemplate.from_template("Question: {question}\n{answer}")
5
6 # 创建示例集
7 examples = [
8 {
9 "question": "李白和杜甫，谁更长寿？",
10 "answer": """
11 是否需要后续问题：是的。
12 后续问题：李白享年多少岁？
13 中间答案：李白享年61岁。
14 后续问题：杜甫享年多少岁？
15 中间答案：杜甫享年58岁。
16 所以最终答案是：李白

17 """
18 },
19 {
20 "question": "腾讯的创始人什么时候出生？",
21 "answer": """
22 是否需要后续问题：是的。
23 后续问题：腾讯的创始人是谁？
24 中间答案：腾讯由马化腾创立。
25 后续问题：马化腾什么时候出生？
26 中间答案：马化腾出生于1971年10月29日。
27 所以最终答案是：1971年10月29日
28 """,
29 },
30 {
31 "question": "孙中山的外祖父是谁？",
32 "answer": """
33 是否需要后续问题：是的。
34 后续问题：孙中山的母亲是谁？
35 中间答案：孙中山的母亲是杨太夫人。
36 后续问题：杨太夫人的父亲是谁？
37 中间答案：杨太夫人的父亲是杨胜辉。
38 所以最终答案是：杨胜辉
39 """,
40 },
41 {
42 "question": "电影《红高粱》和《霸王别姬》的导演来自同一个国家吗？",
43 "answer": """
44 是否需要后续问题：是的。
45 后续问题：《红高粱》的导演是谁？
46 中间答案：《红高粱》的导演是张艺谋。
47 后续问题：张艺来自哪里？
48 中间答案：中国。
49 后续问题：《霸王别姬》的导演是谁？
50 中间答案：《霸王别姬》的导演是陈凯歌。
51 后续问题：陈凯歌来自哪里？
52 中间答案：中国。
53 所以最终答案是：是
54 """,
55 },
56 ]
57 # 测试：实例化第一个示例
58 print(example_prompt.invoke(examples[0]).to_string())
结果如下：
代码块

1 Question: 李白和杜甫，谁更长寿？
2
3 是否需要后续问题：是的。
4 后续问题：李白享年多少岁？
5 中间答案：李白享年61岁。
6 后续问题：杜甫享年多少岁？
7 中间答案：杜甫享年58岁。
8 所以最终答案是：李白
接下来我们需要格式化完整的样本提示。此时可以创建一个 对象来初
FewShotPromptTemplate
始化少样本提示模板。其接收少量示例和少量示例的格式化程序。如下所示：
代码块
1 from langchain_core.prompts import FewShotPromptTemplate
2 prompt = FewShotPromptTemplate(
3 examples=examples,
4 example_prompt=example_prompt, # PromptTemplate，用于格式化单个示例
5 suffix="Question: {input}", # 放在示例之后的提示模板字符串。
6 input_variables=["input"], # 变量的名称列表，这些变量的值需要作为提示词的输
入。
7 )
8 print(
9 prompt.invoke({"input": "《教父》和《星球大战》的导演来自同一个国家
吗？"}).to_string()
10 )
结果如下：
代码块
1 Question: 李白和杜甫，谁更长寿？
2
3 是否需要后续问题：是的。
4 后续问题：李白享年多少岁？
5 中间答案：李白享年61岁。
6 后续问题：杜甫享年多少岁？
7 中间答案：杜甫享年58岁。
8 所以最终答案是：李白
9
10
11 Question: 腾讯的创始人什么时候出生？
12
13 是否需要后续问题：是的。
14 后续问题：腾讯的创始人是谁？
15 中间答案：腾讯由马化腾创立。

16 后续问题：马化腾什么时候出生？
17 中间答案：马化腾出生于1971年10月29日。
18 所以最终答案是：1971年10月29日
19
20
21 Question: 孙中山的外祖父是谁？
22
23 是否需要后续问题：是的。
24 后续问题：孙中山的母亲是谁？
25 中间答案：孙中山的母亲是杨太夫人。
26 后续问题：杨太夫人的父亲是谁？
27 中间答案：杨太夫人的父亲是杨胜辉。
28 所以最终答案是：杨胜辉
29
30
31 Question: 电影《红高粱》和《霸王别姬》的导演来自同一个国家吗？
32
33 是否需要后续问题：是的。
34 后续问题：《红高粱》的导演是谁？
35 中间答案：《红高粱》的导演是张艺谋。
36 后续问题：张艺来自哪里？
37 中间答案：中国。
38 后续问题：《霸王别姬》的导演是谁？
39 中间答案：《霸王别姬》的导演是陈凯歌。
40 后续问题：陈凯歌来自哪里？
41 中间答案：中国。
42 所以最终答案是：是
43
44
45 Question: 《教父》和《星球大战》的导演来自同一个国家吗？
通过为模型提供这样的示例，我们可以引导模型做出更好的响应。
来看下 ，它也实
class langchain_core.prompts.few_shot.FewShotPromptTemplate
现了标准的 Runnable 接口。
类初始化参数说明：
• ：样本示例。
examples
• ：PromptTemplate，用于格式化单个示例
example_prompt
• ：放在示例前面的提示模板字符串。
prefix
• ：放在示例之后的提示模板字符串。
suffix
• ：变量的名称列表，这些变量的值需要作为提示词的输入。
input_variables
类方法说明：

• 方法：此方法与其他 Runnable 实例的   方法类似。输入一个字典给提
.invoke() .invoke()
示符模板，返回完整的提示内容 ：
PromptValue
◦ 的 方法可以将提示值作为【字符串】返回。
PromptValue to_string()
◦ 的 方法可以将提示作为【消息列表】返回。
PromptValue to_messages()
接下来让我们调用聊天模型看看结果，完整代码如下：
代码块
1 from langchain_core.prompts import PromptTemplate
2 from langchain_core.prompts import FewShotPromptTemplate
3 from langchain_openai import ChatOpenAI
4
5 # 创建字符串模板
6 example_prompt = PromptTemplate.from_template("Question: {question}\n{answer}")
7
8 # 创建示例集
9 examples = [
10 {
11 "question": "李白和杜甫，谁更长寿？",
12 "answer": """
13 是否需要后续问题：是的。
14 后续问题：李白享年多少岁？
15 中间答案：李白享年61岁。
16 后续问题：杜甫享年多少岁？
17 中间答案：杜甫享年58岁。
18 所以最终答案是：李白
19 """
20 },
21 {
22 "question": "腾讯的创始人什么时候出生？",
23 "answer": """
24 是否需要后续问题：是的。
25 后续问题：腾讯的创始人是谁？
26 中间答案：腾讯由马化腾创立。
27 后续问题：马化腾什么时候出生？
28 中间答案：马化腾出生于1971年10月29日。
29 所以最终答案是：1971年10月29日
30 """,
31 },
32 {
33 "question": "孙中山的外祖父是谁？",
34 "answer": """
35 是否需要后续问题：是的。
36 后续问题：孙中山的母亲是谁？
37 中间答案：孙中山的母亲是杨太夫人。

38 后续问题：杨太夫人的父亲是谁？
39 中间答案：杨太夫人的父亲是杨胜辉。
40 所以最终答案是：杨胜辉
41 """,
42 },
43 {
44 "question": "电影《红高粱》和《霸王别姬》的导演来自同一个国家吗？",
45 "answer": """
46 是否需要后续问题：是的。
47 后续问题：《红高粱》的导演是谁？
48 中间答案：《红高粱》的导演是张艺谋。
49 后续问题：张艺来自哪里？
50 中间答案：中国。
51 后续问题：《霸王别姬》的导演是谁？
52 中间答案：《霸王别姬》的导演是陈凯歌。
53 后续问题：陈凯歌来自哪里？
54 中间答案：中国。
55 所以最终答案是：是
56 """,
57 },
58 ]
59
60 prompt = FewShotPromptTemplate(
61 examples=examples,
62 example_prompt=example_prompt,
63 suffix="Question: {input}",
64 input_variables=["input"],
65 )
66
67 # 生成 [HumanMessage] 消息列表
68 prompt_messages = prompt.invoke({"input": "《教父》和《星球大战》的导演来自同一个国
家吗？"}).to_messages()
69
70 # 定义大模型
71 model = ChatOpenAI(model="gpt-4o-mini")
72 model.invoke(prompt_messages).pretty_print()
结果如下：
代码块
1 ================================== Ai Message
==================================
2
3 是否需要后续问题：是的。
4 后续问题：《教父》的导演是谁？

5 中间答案：《教父》的导演是弗朗西斯·福特·科波拉。
6 后续问题：弗朗西斯·福特·科波拉来自哪里？
7 中间答案：弗朗西斯·福特·科波拉来自美国。
8 后续问题：《星球大战》的导演是谁？
9 中间答案：《星球大战》的导演是乔治·卢卡斯。
10 后续问题：乔治·卢卡斯来自哪里？
11 中间答案：乔治·卢卡斯也来自美国。
12 所以最终答案是：是
3.3.2 案例二：使用示例数据增强 LangChain 信息提取能力
在我们学习【结构化输出】小节时，其中有一个使用场景是使用结构化输出进行信息提取。当时说明
可以结合少样本提示来实现。于是在这里，我们来实现一个基于 LangChain 的结构化信息提取系统，
专门从文本中提取人物相关信息。
例如我们希望，对于输入以下文本：
"篮球场上，身高两米的中锋王伟默契地将球传给一米七的后卫挚友李明，完成一记绝杀。这对老友
用十年配合弥补了身高的差距。"
代码会提取出结构化数据，如下所示：
代码块
1 people=[
2 Person(name="王伟", height_in_meters="2", skin_color=None, hair_color=None),
3 Person(name="李明", height_in_meters="1.7", skin_color=None,
hair_color=None)
4 ]
实现如下：
第一步：定义结构化返回对象。
代码块
1 from typing import List, Optional
2 from pydantic import BaseModel, Field
3
4 # 定义结构化输出
5 class Person(BaseModel):
6 """一个人的信息。"""
7
8 name: Optional[str] = Field(default=None, description="这个人的名字")

9 hair_color: Optional[str] = Field(default=None, description="如果知道这个人头
发的颜色")
10 skin_color: Optional[str] = Field(default=None, description="如果知道这个人的
肤色")
11 height_in_meters: Optional[str] = Field(default=None, description="以米为单
位的高度")
12
13 class Data(BaseModel):
14 """提取关于人的数据。"""
15
16 people: List[Person]
第二步：定义两个关键示例，每个示例中包含【文本】和【希望输出】：
1. 无人物场景：文本中没有人名，期望返回空列表
2. 部分信息场景：只有名字，其他字段缺失
代码块
1 # 定义2个示例
2 examples = [
3 (
4 "海洋是广阔而蓝色的。它有两万多英尺深。",
5 Data(people=[]), # 没有人物信息的情况
6 ),
7 (
8 "小强从中国远行到美国。",
9 Data(people=[
10 Person(name="小强", height_in_meters=None, skin_color=None,
hair_color=None),
11 ]), # 部分信息缺失的情况
12 ),
13 ]
第三步：定义提示词模板。
代码块
1 from langchain_core.prompts import ChatPromptTemplate, MessagesPlaceholder
2 from langchain_core.messages import HumanMessage, SystemMessage
3
4 # 定义提示词模板
5 prompt_template = ChatPromptTemplate(
6 [
7 SystemMessage(content="你是一个提取信息的专家，只从文本中提取相关信息。"
8 "如果您不知道要提取的属性的值，属性值返回null"),

9 MessagesPlaceholder("example_messages"),
10 ("user", "{new_message}"),
11 ]
12 )
第四步：构造请求的消息列表。处理逻辑：
• 遍历每个示例对：文本、期望输出
• 根据 是否有人员信息 生成自然语言响应： 检测到人  or  未检测到人
" " " "
• 使用 转换格式，将每个示例转换为模型可理解的消息格式
tool_example_to_messages
代码块
1 from langchain_core.utils.function_calling import tool_example_to_messages
2
3 # 样例消息列表
4 example_messages = []
5 # 遍历示例对，将每个示例构造消息
6 for txt, tool_call in examples:
7 # 根据提取结果生成AI响应文本
8 if tool_call.people:
9 ai_response = "检测到人"
10 else:
11 ai_response = "未检测到人"
12
13 # 将示例转换为模型可理解的消息格式
14 example_messages.extend(
15 tool_example_to_messages(
16 txt, [tool_call], ai_response=ai_response
17 ) # 方法返回 list[BaseMessage]
18 )
方法说明：
tool_example_to_messages
• 此功能处于测试阶段。它正在积极开发中，因此 API 可能会发生变化。
• 此方法是一个工具方法，它可以将单个示例转换为聊天模型可以识别的消息列表。
• 参数说明：
◦ ：输入字符串
input
◦ ：list[BaseModel]，表示为 Pydantic BaseModel 的工具调用列表
tool_calls
◦ ：可选的。如果提供，将是最终 AIMessage 的内容。
ai_response
• 返回值：list[BaseMessage] 消息列表

测试一下：
代码块
1 # 实例化提示词
2 formatted_prompt = prompt_template.invoke({
3 "example_messages": example_messages,
4 "new_message": "篮球场上，身高两米的中锋王伟默契地将球传给一米七的后卫挚友李明，完成
一记绝杀。"
5 "这对老友用十年配合弥补了身高的差距。"
6 })
7 # 打印
8 for message in formatted_prompt.to_messages():
9 message.pretty_print()
结果如下：
代码块
1 ================================ System Message
================================
2
3 你是一个提取信息的专家，只从文本中提取相关信息。如果您不知道要提取的属性的值，属性值返回
null
4 ================================ Human Message
=================================
5
6 海洋是广阔而蓝色的。它有两万多英尺深。
7 ================================== Ai Message
==================================
8 Tool Calls:
9 Data (f81f0c81-108d-4c06-88dc-9e5f5289749e)
10 Call ID: f81f0c81-108d-4c06-88dc-9e5f5289749e
11 Args:
12 people: []
13 ================================= Tool Message
=================================
14
15 You have correctly called this tool.
16 ================================== Ai Message
==================================
17
18 未检测到人
19 ================================ Human Message
=================================
20
21 小强从中国远行到美国。

22 ================================== Ai Message
==================================
23 Tool Calls:
24 Data (ace46c82-07e0-455c-aaef-c13da7328e45)
25 Call ID: ace46c82-07e0-455c-aaef-c13da7328e45
26 Args:
27 people: [{'name': '小强', 'hair_color': None, 'skin_color': None,
'height_in_meters': None}]
28 ================================= Tool Message
=================================
29
30 You have correctly called this tool.
31 ================================== Ai Message
==================================
32
33 检测到人
34 ================================ Human Message
=================================
35
36 篮球场上，身高两米的中锋王伟默契地将球传给一米七的后卫挚友李明，完成一记绝杀。这对老友用十
年配合弥补了身高的差距。
消息已经被我们构造成功！可以看到，一个示例经过转换构建出了4条消息： 、
HumanMessage
、 、 。
AiMessage(tool) ToolMessage AiMessage
第五步：初始化模型并绑定结构化输出模式，最后构件链，并调用。
代码块
1 from langchain_openai import ChatOpenAI
2
3 # 定义结构化输出模型，自动解析为 Pydantic 对象
4 model = ChatOpenAI(model="gpt-4o-mini")
5 structured_model = model.with_structured_output(schema=Data)
6
7 chain = prompt_template | structured_model
8 result = chain.invoke({
9 "example_messages": example_messages,
10 "new_message": "篮球场上，身高两米的中锋王伟默契地将球传给一米七的后卫挚友李明，完成
一记绝杀。"
11 "这对老友用十年配合弥补了身高的差距。"
12 })
13 print("\n")
14 print(result)
结果如下：

代码块
1
2 people=[
3 Person(name='王伟', hair_color=None, skin_color=None,
height_in_meters='2.0'),
4 Person(name='李明', hair_color=None, skin_color=None,
height_in_meters='1.7')
5 ]
完整代码：
代码块
1 from langchain_openai import ChatOpenAI
2 from typing import List, Optional
3 from pydantic import BaseModel, Field
4 from langchain_core.prompts import ChatPromptTemplate, MessagesPlaceholder
5 from langchain_core.messages import HumanMessage, SystemMessage
6 from langchain_core.utils.function_calling import tool_example_to_messages
7
8 # 1. 定义结构化输出
9 class Person(BaseModel):
10 """一个人的信息。"""
11
12 name: Optional[str] = Field(default=None, description="这个人的名字")
13 hair_color: Optional[str] = Field(default=None, description="如果知道这个人头
发的颜色")
14 skin_color: Optional[str] = Field(default=None, description="如果知道这个人的
肤色")
15 height_in_meters: Optional[str] = Field(default=None, description="以米为单
位的高度")
16
17 class Data(BaseModel):
18 """提取关于人的数据。"""
19
20 people: List[Person]
21
22 # 2. 定义2个示例
23 examples = [
24 (
25 "海洋是广阔而蓝色的。它有两万多英尺深。",
26 Data(people=[]), # 没有人物信息的情况
27 ),
28 (
29 "小强从中国远行到美国。",
30 Data(people=[

31 Person(name="小强", height_in_meters=None, skin_color=None,
hair_color=None),
32 ]), # 部分信息缺失的情况
33 ),
34 ]
35
36 # 3. 定义提示词模板
37 prompt_template = ChatPromptTemplate(
38 [
39 SystemMessage(content="你是一个提取信息的专家，只从文本中提取相关信息。"
40 "如果您不知道要提取的属性的值，属性值返回null"),
41 MessagesPlaceholder("example_messages"),
42 ("user", "{new_message}"),
43 ]
44 )
45
46 # 4. 获取样例消息列表
47 example_messages = []
48 # 遍历示例对，将每个示例构造成聊天消息
49 for txt, tool_call in examples:
50 # 根据提取结果生成AI响应文本
51 if tool_call.people:
52 ai_response = "检测到人"
53 else:
54 ai_response = "未检测到人"
55
56 # 将示例转换为模型可理解的消息格式
57 example_messages.extend(
58 tool_example_to_messages(
59 txt, [tool_call], ai_response=ai_response
60 ) # 方法返回 list[BaseMessage]
61 )
62
63
64 # 5. 调用
65 # 定义结构化输出模型，自动解析为 Pydantic 对象
66 model = ChatOpenAI(model="gpt-4o-mini")
67 structured_model = model.with_structured_output(schema=Data)
68
69 # 定义链
70 chain = prompt_template | structured_model
71 result = chain.invoke({
72 "example_messages": example_messages,
73 "new_message": "篮球场上，身高两米的中锋王伟默契地将球传给一米七的后卫挚友李明，完成
一记绝杀。"
74 "这对老友用十年配合弥补了身高的差距。"
75 })

76 print("\n")
77 print(result)
总结：该案例展示了 LangChain 在结构化信息提取中的核心能力：通过Pydantic定义输出模式、少样
本学习引导模型行为、提示词模板动态组装上下文，以及链式调用简化流程。最终实现从非结构化文
本中精准提取结构化数据。
3.4 示例选择器（Example selectors）
3.4.1 概念
一旦我们有了示例数据集，就需要考虑提示中应该有多少个示例。关键的权衡是，更多的示例通常会
提高性能，但更大的提示会增加成本和延迟。超过某个阈值，太多示例可能会开始混淆模型。
找到正确数量的示例在很大程度上取决于模型、任务、示例的质量以及成本和延迟限制。有趣的是，
模型越好，它需要精准的示例就越少。但其实，最佳的方法是使用不同数量的示例进行一些实验。
若此时我们有【大量】的示例数据集。对于大模型来说，就没必要全部使用与参考。我们需要有一种
方法可以根据给定的输入，从数据集中选择示例。
在 LangChain 中，示例选择器就可以帮我们从一组【示例的集合】中根据具体策略选择正确的【示例
子集】构建少样本提示。
选择策略有：
• ：根据特定【长度】内可以容纳的数量选择示例。
Length
• ：使用输入和示例之间的【语义相似性】来决定选择哪些示例。
Similarity

• ：使用输入和示例之间的【最大边际相关性】来决定要选择哪些示例。
MMR
• ：使用输入和示例之间的【ngram 重叠】来决定要选择哪些示例。
Ngram
这些其实都是自然语言处理（NLP）里的相似性衡量问题。
3.4.2 按长度选择示例 （Length）
当我们担心构造提示时，将超过上下文窗口长度，根据特定长度内可以容纳的数量选择示例。对于较
长的输入，它将选择更少的示例来包含；而对于较短的输入，它将选择更多示例。
实现按长度选择示例的示例选择器是：
class
langchain_core.example_selectors.length_based.LengthBasedExampleSelecto
类，其参数如下：
r
• ：PromptTemplate，用于格式化示例的提示模板。
example_prompt
• ：模板所需的示例列表。
examples
• ：提示的最大长度，超过该长度将剪切示例。
max_length
• ：测量提示长度的方法。默认为字数统计。
get_text_length
内置方法：
：将新示例添加到列表中。
add_example(example: dict[str, str])
◦ 输入：一个字典，其中键作为输入变量，值作为其值。
select_examples(input_variables: dict[str, str]) → list[dict] ：根据输
入长度选择要使用的示例。
◦ 输入：一个字典，其中键作为输入变量，值作为其值。
◦ 输出：要包含在提示中的示例列表。
接下来，演示一下如何使用长度示例选择器，让我们：
1. 给一个示例集，输入和输出互为反义词
2. 定义 字符串模板，包含输入和输出两个"占位符"
PromptTemplate
3. 定义 长度示例选择器，设置初始示例集与最大长度
LengthBasedExampleSelector
4. 定义一个 模板对象，用于实例化示例，将示例转化为聊天消息
FewShotPromptTemplate
5. 打印消息结果
代码如下：
代码块
1 from langchain_core.example_selectors import LengthBasedExampleSelector

2 from langchain_core.prompts import FewShotPromptTemplate, PromptTemplate
3
4 # 反义词示例集合
5 examples = [
6 {"input": "happy", "output": "sad"},
7 {"input": "tall", "output": "short"},
8 {"input": "energetic", "output": "lethargic"},
9 {"input": "sunny", "output": "gloomy"},
10 {"input": "windy", "output": "calm"},
11 ]
12
13 # 字符串模板
14 example_prompt = PromptTemplate(
15 input_variables=["input", "output"],
16 template="Input: {input}\nOutput: {output}",
17 )
18
19 # 长度示例选择器
20 example_selector = LengthBasedExampleSelector(
21 examples=examples,
22 example_prompt=example_prompt,
23 # 格式化示例的最大长度。
24 # 长度由下面的get_text_length函数测量。
25 max_length=25,
26 # 用于获取字符串长度的函数，用于确定包含哪些示例。
27 # 如果没有指定，它是作为默认值提供的。
28 # 该函数返回一个整数，表示字符串中由换行符或空格分隔的“单词”数量
29 # get_text_length: Callable[[str], int] = lambda x: len(re.split("\n| ",
x))
30 )
31
32 # 用于实例化示例的模板
33 dynamic_prompt = FewShotPromptTemplate(
34 # 提供了一个ExampleSelector而不是examples。
35 example_selector=example_selector,
36 example_prompt=example_prompt,
37 prefix="给出每个输入的反义词",
38 suffix="Input: {adjective}\nOutput:",
39 input_variables=["adjective"],
40 )
41
42 print(
43 dynamic_prompt.invoke({"adjective": "big"}).to_messages()[0].content
44 )
结果如下：

代码块
1 给出每个输入的反义词
2
3 Input: happy
4 Output: sad
5
6 Input: tall
7 Output: short
8
9 Input: energetic
10 Output: lethargic
11
12 Input: sunny
13 Output: gloomy
14
15 Input: windy
16 Output: calm
17
18 Input: big
19 Output:
可以看到，示例选择器将所有的示例都选择了，因为还没有超过设置的示例最大长度，长度计算是根
据字符串中由换行符或空格分隔的“单词”数量得到的（默认）。
接下来让我们加入长输入，验证长度选择器筛选示例。代码如下：
代码块
1 # 一个长的示例输入，所有示例超出最大长度
2 long_string = "非常 非常 非常 非常 非常 非常 非常 非常 非常 非常 非常 非常 非常 非常
非常 非常大，比其他任何东西都要大得多"
3 print(
4 dynamic_prompt.invoke({"adjective": long_string}).to_messages()[0].content
5 )
结果如下：
代码块
1 给出每个输入的反义词
2
3 Input: happy
4 Output: sad
5
6 Input: tall

7 Output: short
8
9 Input: 非常 非常 非常 非常 非常 非常 非常 非常 非常 非常 非常 非常 非常 非常 非常 非常
大，比其他任何东西都要大得多
10 Output:
3.4.3 按语义相似性选择示例（Similarity）
3.4.3.1 概念
什么是语义相似？它是衡量文本在【含义上】的接近程度。例如下述两段文本：
text1 = "我喜欢猫"
text2 = "我讨厌狗"
这两段文本表面相似度低，但语义上都是表达对动物的态度。
再例如：
text1 = "苹果很甜"
text2 = "苹果市值创新高"
"苹果"可以指水果或公司，语义相似可以解决一词多义问题，因此这两段文本语义上不相似。
LangChain 能根据输入和示例之间的语义相似性来决定选择哪些示例，它通过查找与输入具有最大余
弦相似性的嵌入示例来实现这一点。
3.4.3.2 实现
实现按语义相似性选择示例的示例选择器是：
class
langchain_core.example_selectors.semantic_similarity.SemanticSimilarity
类，
ExampleSelector
内置方法：
：根据示例集生成语义相似示例选择器
from_examples()
◦ 输入：
▪ ：示例列表
examples
▪ ：初始化的嵌入 API 接口，如
embeddings OpenAIEmbeddings()
▪ ：向量存储数据库接口类。
vectorstore_cls
▪ ：最终要选择的示例的数量。默认值为 4。
k
◦ 输出：语义相似性示例选择器

：将新示例添加到列表中。
add_example(example: dict[str, str])
◦ 输入：一个字典，其中键作为输入变量，值作为其值。
select_examples(input_variables: dict[str, str]) → list[dict] ：根据输
入选择要使用的示例。
◦ 输入：一个字典，其中键作为输入变量，值作为其值。
◦ 输出：要包含在提示中的示例列表。
接下来，演示一下如何使用语义相似示例选择器，让我们：
1. 给一个示例集，输入和输出互为反义词
2. 定义 字符串模板，包含输入和输出两个"占位符"
PromptTemplate
3. 定义 语义相似示例选择器，设置初始示例集与嵌
SemanticSimilarityExampleSelector
入 API 接口
4. 定义一个 模板对象，用于实例化示例，将示例转化为聊天消息
FewShotPromptTemplate
5. 打印消息结果
代码如下：
注意：运行前先安装 库：
langchain-chroma pip install -U langchain-chroma
代码块
1 from langchain_chroma import Chroma
2 from langchain_core.example_selectors import SemanticSimilarityExampleSelector
3 from langchain_core.prompts import FewShotPromptTemplate, PromptTemplate
4 from langchain_openai import OpenAIEmbeddings
5
6 # 反义词示例集合
7 examples = [
8 {"input": "happy", "output": "sad"},
9 {"input": "tall", "output": "short"},
10 {"input": "energetic", "output": "lethargic"},
11 {"input": "sunny", "output": "gloomy"},
12 {"input": "windy", "output": "calm"},
13 ]
14
15 # 字符串模板
16 example_prompt = PromptTemplate(
17 input_variables=["input", "output"],
18 template="Input: {input}\nOutput: {output}",
19 )
20
21 # 语义相似示例选择器
22 example_selector = SemanticSimilarityExampleSelector.from_examples(

23 examples,
24 # 嵌入类用于生成用于度量语义相似度的嵌入。
25 OpenAIEmbeddings(),
26 # VectorStore类，用于存储嵌入并对其进行相似性搜索。
27 Chroma,
28 # 生成示例的数量。
29 k=1,
30 )
31
32 # 用于实例化示例的模板
33 similar_prompt = FewShotPromptTemplate(
34 # 提供了一个ExampleSelector而不是examples。
35 example_selector=example_selector,
36 example_prompt=example_prompt,
37 prefix="给出每个输入的反义词",
38 suffix="Input: {adjective}\nOutput:",
39 input_variables=["adjective"],
40 )
41
42 print(
43 similar_prompt.invoke({"adjective": "worried"}).to_messages()[0].content
44 )
结果如下：
代码块
1 给出每个输入的反义词
2
3 Input: happy
4 Output: sad
5
6 Input: worried
7 Output:
我们输入的内容和最后选择的示例都表示心情，在语义上最相似！
3.4.4 按最大边际相关性选择示例（MMR）
3.4.4.1 概念
什么是最大边际相关性？它是一种重新排序算法，它使用语义相似性作为基础工具，从一个候选集中
挑选出一组既能代表查询主题又彼此多样化的结果。
听起来好像和语义相似性类似，用一个例子看下两者的区别：

• 【语义相似性】就像面试官衡量每个应聘者与职位要求的匹配度。他会给每个应聘者打一个分数。
• 【最大边际相关性】就像团队经理（MMR算法）要组建一个团队。目标是选出一组“精华”结果，
而不是一个单一结果：
◦ 每个成员都要满足基本职位要求（满足相关性）。
◦ 但经理不希望团队里全是只会一种技能的程序员。他需要前端、后端、算法、测试等不同专长
的人，以确保团队能力全面、减少冗余（新颖性/多样性）。
◦ 经理的策略是：先招一个最匹配的技术大牛（第一步），然后接下来招的人，既要技术达标，
又要和已招的人技能互补（迭代过程）。
了解下使用最大边际相关性的场景，更能让我们理解其概念：
• 语义相似性使用场景：搜索引擎的基础排序、重复检测、聚类、语义搜索。
• MMR 使用场景：
◦ 推荐系统：推荐与用戶兴趣相关但又不同类型的物品，避免“信息茧房”。
◦ 文档摘要：从长文档中选择能代表主旨又包含不同信息的句子，避免摘要内容重复。
◦ RAG (检索增强生成)：在从知识库检索完一堆相关文档后，使用 MMR 进行去重和多样化筛选，
再交给LLM生成答案，能有效提升答案质量和减少幻觉。
3.4.4.2 实现
了解了相关概念后，LangChain 提供了按最大边际相关性选择示例的能力，该示例选择器是：
class
langchain_core.example_selectors.semantic_similarity.MaxMarginalRelevan
类，内置方法：
ceExampleSelector
：根据示例集生成 MMR 示例选择器
from_examples()
◦ 输入：
▪ ：示例列表
examples
▪ ：初始化的嵌入 API 接口，如
embeddings OpenAIEmbeddings()
▪ ：向量存储数据库接口类。
vectorstore_cls
▪ ：最终要选择的示例的数量。默认值为 4。
k
◦ 输出：MMR 示例选择器
：将新示例添加到列表中。
add_example(example: dict[str, str])
◦ 输入：一个字典，其中键作为输入变量，值作为其值。
select_examples(input_variables: dict[str, str]) → list[dict] ：根据输
入选择要使用的示例。

◦ 输入：一个字典，其中键作为输入变量，值作为其值。
◦ 输出：要包含在提示中的示例列表。
接下来，演示一下如何使用 MMR 示例选择器，让我们：
1. 给一个示例集，输入和输出互为反义词
2. 定义 字符串模板，包含输入和输出两个"占位符"
PromptTemplate
3. 定义 MMR 示例选择器，设置初始示例集与嵌入
MaxMarginalRelevanceExampleSelector
API 接口
4. 定义一个 模板对象，用于实例化示例，将示例转化为聊天消息
FewShotPromptTemplate
5. 打印消息结果
代码如下：
代码块
1 from langchain_chroma import Chroma
2 from langchain_core.example_selectors import
MaxMarginalRelevanceExampleSelector
3 from langchain_core.prompts import FewShotPromptTemplate, PromptTemplate
4 from langchain_openai import OpenAIEmbeddings
5
6 # 反义词示例集合
7 examples = [
8 {"input": "happy", "output": "sad"},
9 {"input": "tall", "output": "short"},
10 {"input": "energetic", "output": "lethargic"},
11 {"input": "sunny", "output": "gloomy"},
12 {"input": "windy", "output": "calm"},
13 ]
14
15 # 字符串模板
16 example_prompt = PromptTemplate(
17 input_variables=["input", "output"],
18 template="Input: {input}\nOutput: {output}",
19 )
20
21 # MMR 示例选择器
22 example_selector = MaxMarginalRelevanceExampleSelector.from_examples(
23 examples,
24 OpenAIEmbeddings(),
25 Chroma,
26 k=2,
27 )
28
29 # 用于实例化示例的模板

30 similar_prompt = FewShotPromptTemplate(
31 # 提供了一个ExampleSelector而不是examples。
32 example_selector=example_selector,
33 example_prompt=example_prompt,
34 prefix="给出每个输入的反义词",
35 suffix="Input: {adjective}\nOutput:",
36 input_variables=["adjective"],
37 )
38
39 print(
40 similar_prompt.invoke({"adjective": "worried"}).to_messages()[0].content
41 )
结果如下：
代码块
1 给出每个输入的反义词
2
3 Input: happy
4 Output: sad
5
6 Input: windy
7 Output: calm
8
9 Input: worried
10 Output:
3.4.5 通过 ngram 重叠选择示例（Ngram）
3.4.5.1 概念
什么是【ngram】？ngram 指一个文本序列中连续的 n 个词（word） 或字符（character）。
什么是【ngram 重叠】？通过计算它们之间共同拥有的 ngram 数量来一种衡量两段文本相似度的方
法。
例如下述两段文本：
text1 = "苹果手机很好用"       (分词后:  苹果 手机 很 好用 )
text2 = "这款手机很好用"       (分词后:  这款 手机 很 好用 )
这两段文本单词重复度很高，连续三个词的相同的情况也存在，因此 ngram 重叠高。
再看个例子：

text1 = 苹果手机很好用"        (分词后:  苹果 手机 很 好用 )
text2 = "iPhone 非常不错"     (分词后:  非常 不错 )
iPhone
这两段文本在含义上非常相似，但它们的 ngram 重叠度为 0。
因此，传统 ngram 重叠是一种表面形式的匹配。它只关心词是否完全一样，但对于同义词却无法处
理。
什么是【语义 ngram 重叠】？不再比较词本身，而是比较词背后的语义向量（Embedding）。也就
是说，它不是看两个词 苹果 和 的字面是否相同，而是计算它们在语义空间中的向量是
[ ] [iPhone]
否相似。如果相似度超过某个阈值，就认为它们“重叠”了。还是看这个例子：
text1 = 苹果手机很好用"        (分词后:  苹果 手机 很 好用 )
text2 = "iPhone 非常不错"     (分词后:  非常 不错 )
iPhone
• 计算  苹果  和   的向量相似度 → 得分 0.95 (很高，视为重叠)
iPhone
• 计算  手机  和   的向量相似度 → 得分 0.88 (很高，但可能不会同时计分，取决于算法设
iPhone
计，避免重复计算)
• 计算  很  和  非常  的向量相似度 → 得分 0.90 (很高，视为重叠)
• 计算  好用  和  不错  的向量相似度 → 得分 0.82 (很高，视为重叠)
• 最终，语义上的 unigram 重叠度可能为 3 或 4（非常相似！）。
那么语义 ngram 重叠的使用场景是什么？语义 ngram 重叠常用于需要更精准语义评估的场景，例如
剽窃检测 ， 能够发现那些改换了词汇但保留了核心思想的“智能”剽窃。
3.4.5.2 实现
LangChain 实现按语义 ngram 重叠选择示例的示例选择器是：
class
langchain_community.example_selectors.ngram_overlap.NGramOverlapExample
类，其参数如下：
Selector
• ：PromptTemplate，用于格式化示例的提示模板。
example_prompt
• ：模板所需的示例列表。
examples
• ：算法停止的阈值。默认设置为 -1.0。
threshold
◦ 对于负阈值：按 [重叠分数] 对示例进行排序，但不排除任何示例。
◦ 对于等于 0.0 的阈值：按 [重叠分数] 进行排序，并排除与输入没有 ngram 重叠的示例。
◦ 对于大于 1.0 的阈值：排除所有示例，并返回一个空列表。
内置方法：

• ：将新示例添加到列表中。
add_example(example: dict[str, str])
◦ 输入：一个字典，其中键作为输入变量，值作为其值。
• select_examples(input_variables: dict[str, str]) → list[dict] ：返回
根据输入得到的重叠分数排序的降序示例列表。
◦ 输入：一个字典，其中键作为输入变量，值作为其值。
◦ 输出：要包含在提示中的示例列表。
接下来，演示一下如何使用语义相似示例选择器，让我们：
1. 给一个示例集，设置相关文本
2. 定义 字符串模板，包含输入和输出两个"占位符"
PromptTemplate
3. 定义 示例选择器，设置初始示例集与阈值（-1），表示对
NGramOverlapExampleSelector
示例进行排序，但不排除任何示例。
4. 定义一个 模板对象，用于实例化示例，将示例转化为聊天消息
FewShotPromptTemplate
5. 打印消息排序结果
代码如下：
注意：
• 运行前先安装  库： 。它是自然语言处理工具包，在 NLP 领域中
nltk pip install nltk
最常使用的一个Python库。
• 运行前先安装 库：
langchain_community pip install -U
langchain_community
代码块
1 from langchain_community.example_selectors import NGramOverlapExampleSelector
2 from langchain_core.prompts import FewShotPromptTemplate, PromptTemplate
3
4 # 翻译示例
5 examples = [
6 {"input": "See Spot run.", "output": "看见Spot跑。"},
7 {"input": "My dog barks.", "output": "我的狗叫。"},
8 {"input": "Spot can run.", "output": "Spot可以跑。"},
9 ]
10 # 字符串模板
11 example_prompt = PromptTemplate(
12 input_variables=["input", "output"],
13 template="Input: {input}\nOutput: {output}",
14 )
15
16 # NGram 示例选择器
17 example_selector = NGramOverlapExampleSelector(
18 examples=examples,

19 example_prompt=example_prompt,
20 threshold=-1.0,
21 )
22
23 # 用于实例化示例的模板
24 dynamic_prompt = FewShotPromptTemplate(
25 example_selector=example_selector,
26 example_prompt=example_prompt,
27 prefix="给出每个输入的中文翻译",
28 suffix="Input: {sentence}\nOutput:",
29 input_variables=["sentence"],
30 )
31
32 # 按照重叠分数排序
33 print(
34 dynamic_prompt.invoke({"sentence": "Spot can run fast."}).to_messages()
[0].content
35 )
结果如下：
代码块
1 给出每个输入的中文翻译
2
3 Input: Spot can run.
4 Output: Spot可以跑。
5
6 Input: See Spot run.
7 Output: 看见Spot跑。
8
9 Input: My dog barks.
10 Output: 我的狗叫。
11
12 Input: Spot can run fast.
13 Output:
14
可以看到结果集是按照 ngram 重叠分数进行了排序。
可以分别设置  和 进行测试，打印结果如下：
threshold=0.0 1.0
代码块
1 threshold=0.0 （表示排除不相关的示例）
2 给出每个输入的中文翻译

3
4 Input: Spot can run.
5 Output: Spot可以跑。
6
7 Input: See Spot run.
8 Output: 看见Spot跑。
9
10 Input: Spot can run fast.
11 Output:
12
13 threshold=1.0（排除所有示例）
14 给出每个输入的中文翻译
15
16 Input: Spot can run fast.
17 Output:
4. 输出解析器（Output parsers）
4.1 概念
负责获取模型的输出，并将输出转换为更结构化的格式。当使用 LLM 生成结构化数据或规范化聊天模
型和 LLM 的输出时，这很有用。
大型语言模型（LLM）的输出本质上是非结构化的文本。但在构建应用程序时，我们通常希望得到结
构化的、机器可读的数据，这样可以将其转换为更适合下游任务的格式，比如：
• JSON 对象
• Python 字典或列表
• 一个特定的 Pydantic 模型实例
• 一个简单的布尔值或字符串枚举
输出解析器的作用就是架起这座桥梁：它们将 LLM 的非结构化文本输出转换为结构化格式。这使得与
LLM 的交互从“模糊的文本对话”变成了“精确的数据 API 调用”，是构建可靠、高效 LLM 应用不可
或缺的组件。
4.1.1 与with_structured_output() 的区别
他们都实现了相同的功能，但
• 维度不同：输出解析器是 LangChain 中的一个功能性组件， 是
with_structured_output()
聊天模型的一个方法。

• 用法不同：输出解析器的工作流程可以是链式的，可以将 Prompt、LLM 和 Parser 像管道一样连接
起来： 。而
chain = prompt | llm | parser
却不行，只能手动调用，返回一个新的、具备了结构
with_structured_output(schema)
化输出能力的 Runnable（可运行对象）。
可以根据自己的使用场景选择，如果想使用链式就选择输出解析器。
4.2 解析文本输出
其实对于使用 输出解析器输出文本，我们已经使用过多次了。对于
StrOutputParser
，它也实现了标准的 Runnable 接口。示例如下：
StrOutputParser
代码块
1 from langchain_openai import ChatOpenAI
2 from langchain_core.output_parsers import StrOutputParser
3
4 model = ChatOpenAI(model="gpt-4o-mini")
5 chain = model | StrOutputParser()
6 for chunk in chain.stream("写一首夏天的诗词，50字以内。"):
7 print(chunk, end="|")
输出如下：
代码块
1 |炎|夏|骄|阳|照|，|绿|树|映|蓝|天|。|
2 |蝉|鸣|声|声|烈|，|荷|塘|映|清|鲜|。|
3 |微|风|拂|面|过|，|凉|意|透|心|间|。|
4 |烦|忧|随|汗|去|，|畅|享|此|夏|欢|。||
若是不使用输出解析器，而是直接得到聊天模型返回的 AIMessage，文本内容则需要从消息中的
字段获取。
content
4.3 解析结构化对象输出
要输出结构化对象，需要用到的输出解析器是 。
PydanticOutputParser
 类，其
class langchain_core.output_parsers.pydantic.PydanticOutputParser
参数如下：
• ：要解析的 pydantic 模型。
pydantic_object
内置方法：

：将单个输入转换为输出。
invoke()
get_format_instructions() → str ：重要！！
• 作用：生成一个指令字符串，这个字符串会被添加到发送给 LLM 的提示（Prompt）的末尾。
• 目的：告诉 LLM 应该以什么样的格式返回它的响应。例如，“请将你的回复封装在 XML 标签
中”或“请以 JSON 格式输出，包含 ‘name’ 和 ‘age’ 两个字段”。
代码示例如下：
代码块
1 from langchain_openai import ChatOpenAI
2 from langchain_core.output_parsers import PydanticOutputParser
3 from langchain_core.prompts import PromptTemplate
4 from typing import Optional
5 from pydantic import BaseModel, Field
6
7 # 定义大模型
8 model = ChatOpenAI(model="gpt-4o-mini")
9
10 # 定义输出结构：Pydantic 类
11 class Joke(BaseModel):
12 """给用戶讲一个笑话。"""
13
14 setup: str = Field(description="这个笑话的开头")
15 punchline: str = Field(description="这个笑话的妙语")
16 rating: Optional[int] = Field(
17 default=None, description="从1到10分，给这个笑话评分"
18 )
19
20 # 设置解析器
21 parser = PydanticOutputParser(pydantic_object=Joke)
22
23 # 提示词模板
24 prompt = PromptTemplate(
25 template="Answer the user query.\n{format_instructions}\n{query}\n",
26 input_variables=["query"],
27 # partial_variables：提示模板携带的部分变量的字典，无需在每次调用提示时都传入它们。
28 # 类型为 Mapping[str, Any]，传入template携带的部分变量的字典。
29 partial_variables={"format_instructions":
parser.get_format_instructions()},
30 )
31
32 chain = prompt | model | parser
33 for chunk in chain.stream({"query": "给我讲一个关于唱歌的笑话"}):
34 print(chunk, end="|")

结果如下：
代码块
1 setup='为什么歌手总是带着铅笔去演出？' punchline='' rating=None|
2 setup='为什么歌手总是带着铅笔去演出？' punchline='因为' rating=None|
3 setup='为什么歌手总是带着铅笔去演出？' punchline='因为他们' rating=None|
4 setup='为什么歌手总是带着铅笔去演出？' punchline='因为他们想' rating=None|
5 setup='为什么歌手总是带着铅笔去演出？' punchline='因为他们想要' rating=None|
6 setup='为什么歌手总是带着铅笔去演出？' punchline='因为他们想要不断' rating=None|
7 setup='为什么歌手总是带着铅笔去演出？' punchline='因为他们想要不断调整' rating=None|
8 setup='为什么歌手总是带着铅笔去演出？' punchline='因为他们想要不断调整音' rating=None|
9 setup='为什么歌手总是带着铅笔去演出？' punchline='因为他们想要不断调整音调'
rating=None|
10 setup='为什么歌手总是带着铅笔去演出？' punchline='因为他们想要不断调整音调！'
rating=None|
4.4 解析 JSON 输出
要输出 JSON 格式，需要用到的输出解析器是 。
JsonOutputParser
 类，其参数如下：
class langchain_core.output_parsers.json.JsonOutputParser
• ：用于验证的 Pydantic 对象。如果为空，则不执行任何验证。
pydantic_object
内置方法：
：将单个输入转换为输出。
invoke()
get_format_instructions() → str ：重要！！
• 作用：生成一个指令字符串，这个字符串会被添加到发送给 LLM 的提示（Prompt）的末尾。
• 目的：告诉 LLM 应该以什么样的格式返回它的响应。例如，“请将你的回复封装在 XML 标签
中”或“请以 JSON 格式输出，包含 ‘name’ 和 ‘age’ 两个字段”。
代码示例如下，这将提示模型返回 JSON：
代码块
1 from langchain_openai import ChatOpenAI
2 from langchain_core.output_parsers import JsonOutputParser
3 from langchain_core.prompts import PromptTemplate
4
5 # 定义大模型
6 model = ChatOpenAI(model="gpt-4o-mini")
7
8 # 设置解析器

9 parser = JsonOutputParser()
10
11 # 提示词模板
12 prompt = PromptTemplate(
13 template="Answer the user query.\n{format_instructions}\n{query}\n",
14 input_variables=["query"],
15 partial_variables={"format_instructions":
parser.get_format_instructions()},
16 )
17
18 chain = prompt | model | parser
19 print(chain.invoke({"query": "给我讲一个关于唱歌的笑话"}))
结果如下：
代码块
1 {'joke': '为什么歌手总是带着梯子？\n因为他们想要在音乐会上达到更高的层次！'}
也可以使用带 Pydantic 情况下的  ，如下所示：
JsonOutputParser
代码块
1 from langchain_openai import ChatOpenAI
2 from langchain_core.output_parsers import JsonOutputParser
3 from langchain_core.prompts import PromptTemplate
4 from typing import Optional
5 from pydantic import BaseModel, Field
6
7 # 定义大模型
8 model = ChatOpenAI(model="gpt-4o-mini")
9
10 # 定义输出结构：Pydantic 类
11 class Joke(BaseModel):
12 """给用戶讲一个笑话。"""
13
14 setup: str = Field(description="这个笑话的开头")
15 punchline: str = Field(description="这个笑话的妙语")
16 rating: Optional[int] = Field(
17 default=None, description="从1到10分，给这个笑话评分"
18 )
19
20 # 设置解析器
21 parser = JsonOutputParser(pydantic_object=Joke)
22

23 # 提示词模板
24 prompt = PromptTemplate(
25 template="Answer the user query.\n{format_instructions}\n{query}\n",
26 input_variables=["query"],
27 partial_variables={"format_instructions":
parser.get_format_instructions()},
28 )
29
30 chain = prompt | model | parser
31 print(chain.invoke({"query": "给我讲一个关于唱歌的笑话"}))
结果如下：
代码块
1 {'setup': '为什么歌手从不在森林里唱歌？', 'punchline': '因为他们会被树木的‘静’止
住！', 'rating': None}
除了上面讲的文本、对象、JSON解析器，其实 LangChain 官方还提供了更多类型的解析器，如:
• XML 解析器：
XMLOutputParser
• Yaml 解析器：
YamlOutputParser
• CSV 解析器：
CommaSeparatedListOutputParser
• 枚举解析器：
EnumOutputParser
• 日期解析器： 等等，更多类型参考这里。
DatetimeOutputParser
除此之外，LangChain 还支持我们自定义输出解析器，以将模型输出结构化为自定义格式，详细情况
参考这里。
5. 文档加载器（Document loaders）
5.1 RAG 介绍
5.1.1 RAG 概念
从本小节开始，我们将重点放在 RAG 阶段（Retrieval-Augmented Generation，检索增强生成）。
这是当前大语言模型应用的核心模式。RAG 的流程相对复杂，为了更好的理解 RAG，我们先用 AI 搜索
来引出 RAG。
• 对于【AI 大模型】来说，它最擅长的是语义理解和文本总结，最不擅长的就是获取实时的信息。因
为大模型的训练数据是有截止日期的！

• 对于【搜索引擎】来说，它最擅长的就是获取实时的信息，缺点是信息分散，每次都需要人为进行
总结。
• 大模型与搜索引擎的结合，就是给 AI 配备了一个活字典，让 AI 可以随时进行查阅。
下图展示了一个最简单的 AI 搜索工作流程，搜索引擎在这里充当知识库，结合我们的查询语句，大模
型便可以从知识库中获取相应的查询结果：
有了以上流程的铺垫，接下来，正式进入 RAG 的学习。
首先，先来思考一个问题：搜索引擎可以帮我们解决实时数据的获取，但获取到的数据也是受限的。
它只能获取到公开在网络中的数据，而无法获取到一些本地数据，或企业内部的私有数据等，此时该
如何？
答案是使用 RAG（检索增强生成）技术！当用戶向 LLM 提问时，系统首先在知识库（如公司内部文
档）中进行语义搜索，找到最相关的内容，然后将这些内容和问题一起交给 LLM 来生成答案。与 AI 搜
索类比，本质是知识库改变了，从搜索引擎线上搜索改为了本地或私有知识库中搜索。

5.1.2 RAG 流程
RAG 的流程分为【离线数据处理】和【在线检索】两个过程。
上面提到，RAG 知识库可以是本地文档、公司内部文档等一些私有化数据。但这些私有数据或文档实
际上并不能很好地被直接进行检索访问。因此需要将这些私有化数据构建成可以被检索的知识库，这
就是离线数据处理要干的事情。经过离线数据后，知识则会按照某种格式以及排列方式存储在知识库
中，等待被使用。
而在线检索则是我们依赖知识库查询，通过大模型生成结果的过程。
过程如下图所示：
这张图将会是我们后续要学习 LangChain 组件知识地图，所有的组件都会一一进行讲解，现在我们只
需掌握其流程，接触相关概念即可。
• 文档加载 (Document Loading)：加载多种不同来源加载文档。LangChain 提供了 100 多种不同的
文档加载器，包括 PDF 在内的非结构化的数据、SQL 在内的结构化的数据，以及 Python、Java
之类的代码等。
• 文本分割 (Splitting)：文本分割器把 Documents 切分为指定大小的块。
• 存储 (Storage)：存储涉及到两个环节，分别是：
◦ 将切分好的文档块进行嵌入（Embedding），即将文档块转换成向量的形式。
◦ 将 Embedding 后的向量数据，存储到向量数据库中。
• 检索 (Retrieval)：数据存入向量数据库后。当我们需要进行数据检索时，会通过某种检索算法找到
与输入问题相似的文档块。
• 输出 (Output)：把问题以及检索出来的文档块一起提交给 LLM，LLM 会通过问题和检索出来的提
示一起来生成更加合理的答案。

我们现在已经知道了 RAG 的完整流程，但也仅是知道 RAG 是什么，至于流程中为什么要进行文档加
载、文本分割、存储，我们还无从了解。因此后续的各个 LangChain 组件讲解时都会涉及每个步骤被
设计出来的原因。
5.1.3 RAG 示例
讲这么多，不如一见。接下来就来演示一个 RAG 系统。在这个示例中，我们
• 提供了：《租房项目Q&A》文档
• 希望：通过聊天方式，提问关于项目的任何问题，最终得到答案。
• 要求：最多只用三句话回答，要简明扼要。
代码如下（不用理解代码含义，只需要看结果即可）：
代码块
1 from langchain_openai import OpenAIEmbeddings, ChatOpenAI
2 from langchain_redis import RedisConfig, RedisVectorStore
3 from langchain_core.output_parsers import StrOutputParser
4 from langchain_core.prompts import ChatPromptTemplate
5 from langchain_core.runnables import RunnablePassthrough
6
7 # 定义聊天模型
8 model = ChatOpenAI(model="gpt-4o-mini")
9 # 定义嵌入模型
10 embeddings = OpenAIEmbeddings(model="text-embedding-3-large")
11
12 # 配置 Redis 客戶端
13 redis_url = "redis://192.168.100.238:6379"
14 config = RedisConfig(
15 index_name="qa",
16 redis_url=redis_url,
17 metadata_schema=[
18 {"name": "category", "type": "tag"},
19 {"name": "num", "type": "numeric"},
20 ],
21 )
22 # 定义 Redis 向量存储
23 vector_store = RedisVectorStore(embeddings, config=config)
24 # 生成检索器
25 retriever = vector_store.as_retriever()
26
27 # 定义提示词模板
28 prompt = ChatPromptTemplate.from_messages(
29 [
30 (

31 "human",
32 """你是负责回答问题的助手。使用以下检索到的上下文片段来回答问题。如果你不知
道答案，就说你不知道。最多只用三句话，回答要简明扼要。
33 Question: {question}
34 Context: {context}
35 Answer:""",
36 ),
37 ]
38 )
39
40 # 将文档转换为字符串
41 def format_docs(docs):
42 return "\n\n".join(doc.page_content for doc in docs)
43
44 # 定义链
45 rag_chain = (
46 {"context": retriever | format_docs, "question": RunnablePassthrough()}
47 | prompt
48 | model
49 | StrOutputParser()
50 )
51
52 # 循环输入问题
53 while True:
54 # 获取用戶输入
55 question = input("\n请输入您的问题（输入'退出'或'quit'结束程序）: ").strip()
56
57 # 检查是否退出
58 if question.lower() in ["退出", "quit"]:
59 print("程序已结束，再见！")
60 break
61
62 # 检查输入是否为空
63 if not question:
64 print("问题不能为空，请重新输入。")
65 continue
66
67 # 执行链，流式输出
68 print("回答: ", end="", flush=True)
69 chunks = []
70 for chunk in rag_chain.stream(question):
71 chunks.append(chunk)
72 print(chunk, end="", flush=True)
73 print() # 换行
运行代码：

代码块
1 请输入您的问题（输入'退出'或'quit'结束程序）: 介绍一下这个项目
2 回答: 这个项目是一个基于脚手架的微服务在线租房系统，旨在模仿贝壳、安居客等流行应用，具备真
实的交互体验和架构设计。通过结合理论和实践，我在这个项目中加深了对 Java 编程语言的理解。这
个项目的灵感来源于我在大学时和同学合租的经历。
3
4 请输入您的问题（输入'退出'或'quit'结束程序）: 项目设计难点有哪些？
5 回答: 项目设计的难点主要包括非技术方面的需求把控和技术方面的明确项目范围与目标。缺乏产品经
理的介入使得设计过程中需自行绘制原型图并拆分功能，这增加了设计的复杂性。同时，测试和质量保
证也是项目中的重要挑战，需要合理安排时间以确保项目质量。
6
7 请输入您的问题（输入'退出'或'quit'结束程序）: 介绍数据存储的相关设计
8 回答: 数据存储的设计中，使用MySQL作为关系数据库管理系统，并结合MyBatis简化SQL操作。为了
更好地应对高并发场景，设计了Redis缓存方案来优化内存利用和减轻数据库压力。引入OSS对象存储
是为了实现无限扩展性，相对于本地存储和MySQL，它具备更高的性能和成本效益。
9
10 请输入您的问题（输入'退出'或'quit'结束程序）: 详细介绍 Redis 与 Mysql 数据一致性方案
11 回答: Redis与MySQL的数据一致性方案通常采用“双写一致性”模式，通过Cache-Aside方法实现。在
这一方案中，应用程序在读取数据时先查询Redis缓存，如果不存在再查询MySQL数据库，并将结果缓
存到Redis中；在更新数据时，需同时更新MySQL和Redis，以确保两者状态一致。此方法也包括设置
合理的缓存过期时间和使用布隆过滤器，以解决缓存穿透等问题。
12
13 请输入您的问题（输入'退出'或'quit'结束程序）:
我们提供的文档越详细，生成的结果越符合预期。
5.2 Document 文档类
要想实现 RAG，首先就需要从源中获取数据，即加载数据或文档。这是通过 LangChain 的文档加载器
完成的。LangChain 文档加载器可以将各种数据源加载成一系列的文档对象 。
Document
用于存储一段文本和相关元数据的
class langchain_core.documents.base.Document
类，我们可以直接定义LangChain 文档列表，如下所示：
代码块
1 from langchain_core.documents import Document
2 documents = [
3 # 单个Document对象通常表示较大文档的一个块
4 Document(
5 # 内容字符串
6 page_content="狗是很好的伴侣，以忠诚和友好而闻名。",
7 # 元数据字典
8 # 元数据属性可以捕获有关文档源、与其他文档的关系以及其他信息的信息。

9 metadata={"source": "mammal-pets-doc"},
10 ),
11 Document(
12 page_content="猫是独立的宠物，经常享受自己的空间。",
13 metadata={"source": "mammal-pets-doc"},
14 ),
15 ]
这里我们定义了一个 文档列表，其内包含了两个 文档对象。通常，单个
documents Document
Document对象表示较大文档的一个块/页。每个 对象，包含了以下参数：
Document
• ：可选的文档标识符。理想情况下，这应该在整个文档集合中是唯一的，并格式化为
id
UUID，但不会强制执行。
• ：字符串文本
page_content
• ：与内容关联的任意元数据。类型为
metadata dict [Optional]
5.3 加载 PDF 文档
将本地的 PDF 文档加载到 LangChain 中，其实就是将 PDF 文档转换为一个个 对象。这时
Document
就需要我们使用 文档加载器完成这一功能。
PyPDFLoader
类，有以下关键
class langchain_community.document_loaders.pdf.PyPDFLoader
函数：
• 初始化函数，入参 ，表示要加载的 PDF 文件的路径。
init() file_path
• load() → list[Document] ：将数据加载到文档对象中。返回文档对象列表。
现在，让我们加载一个本地 PDF 文档看下效果。
代码块
1 from langchain_community.document_loaders import PyPDFLoader
2 file_path = "../Docs/PDF/脚手架级微服务租房平台Q&A.pdf"
3 loader = PyPDFLoader(file_path)
4 # 将 PDF 文件的每一页转换为一个独立的 Document 对象，并存储在列表 docs 中。
5 docs = loader.load()
接下来，来看看 重要的一些参数：
Document
代码块
1 print(f"问：PDF 文件的总页数为：\n{len(docs)}\n")
2 print(f"问：第一页文本内容的前200个字符是：\n{docs[0].page_content[:200]}\n")
3 print(f"问：第一页元数据：\n{docs[0].metadata}")

打印结果如下：
代码块
1 问：PDF 文件的总页数为：
2 32
3
4 问：第一页文本内容的前200个字符是：
5 脚手架级微服务租房平台
6 通用问题
7 1. 为什么做这个项目？
8 • 回答1：（出于兴趣爱好开发）
9 大学期间，我和同学在外合租过一段时间，使用了一些租房平台，于是我有个想法，自己能不能开发
10 一个租房平台，可以让我将理论知识与实践相结合。我希望通过实际项目来加深对Java编程语言和相
11 关技术的理解。于是我便查找了一些资料，看了一些开源项目，进行了一些改进。
12 • 回答2：（开源项目的解释）
13 这个
14
15 问：第一页元数据：
16 {'producer': 'pdfcpu v0.8.1 dev', 'creator': 'Chromium', 'creationdate':
'2025-08-28T17:52:34+08:00', 'moddate': '2025-08-28T17:52:34+08:00', 'source':
'../PDF/脚手架级微服务租房平台Q&A.pdf', 'total_pages': 32, 'page': 0,
'page_label': '1'}
17
现在许多 LLM 支持对多模态输入（例如图像）进行推理。在某些应用程序中，例如对具有复杂布局、
图表或扫描的 PDF 进行问答，可以跳过 PDF 解析，直接将 PDF 页面转换为图像并将其直接传递给模
型可能是更准确的。
5.4 加载 Markdown 文件
将本地的 文档加载到 LangChain 中，需要我们使用
Markdown
文档加载器完成这一功能。
UnstructuredMarkdownLoader
class
langchain_community.document_loaders.markdown.UnstructuredMarkdownLoade
类，有以下关键函数：
r
• 初始化函数，所需参数：
init()
◦ ：表示要加载的 文件的路径。
file_path Markdown
◦ ：加载文件时要使用的模式。可以是 single 或 elements。默认为 single。
mode
single：文档将作为单个 Document 对象返回
▪

s gle：文档将作为单个  ocu e t 对象返回
▪
▪ elements：会将文档拆分为 Title 和 NarrativeText 等不同类型的元素。
• load() → list[Document] ：将数据加载到文档对象中。返回文档对象列表。
LangChain 实现的 需要依赖 Unstructured 包。因此在使用前我
UnstructuredMarkdownLoader
们需要先安装它：
代码块
1 pip install "unstructured[md]" nltk
现在，让我们使用 模式加载一个本地 文档看下效果。
single Markdown
代码块
1 from langchain_community.document_loaders import UnstructuredMarkdownLoader
2 from langchain_core.documents import Document
3
4 markdown_path = "../Docs/Markdown/脚手架级微服务租房平台Q&A.md"
5 # single 模式，加载后，默认只有一个 Document 对象
6 loader = UnstructuredMarkdownLoader(markdown_path)
7 data = loader.load()
8
9 assert len(data) == 1
10 assert isinstance(data[0], Document)
11 print(data[0].page_content[:200])
12 print(data[0].metadata)
打印结果如下：
代码块
1 通用问题
2
3 为什么做这个项目？
4
5 回答1
6 {'source': '../Docs/Markdown/脚手架级微服务租房平台Q&A.md'}
接下来再看下 模式下加载本地 文档的效果：
elements Markdown
代码块
1 from langchain_community.document_loaders import UnstructuredMarkdownLoader
2 from langchain_core.documents import Document
3

4 markdown_path = "../Docs/Markdown/脚手架级微服务租房平台Q&A.md"
5 # single 模式，加载后，默认只有一个 Document 对象
6 loader = UnstructuredMarkdownLoader(markdown_path, mode="elements")
7 data = loader.load()
8
9 print(f"问：文档个数为：\n{len(data)}\n")
10 print("问：前三个文档数据：")
11 for document in data[:3]:
12 print(f"{document}\n")
打印结果如下：
代码块
1 问：文档个数为：
2 441
3
4 问：前三个文档数据：
5 page_content='通用问题' metadata={'source': '../Docs/Markdown/脚手架级微服务租房平
台Q&A.md', 'category_depth': 0, 'languages': ['zho'], 'file_directory':
'../Docs/Markdown', 'filename': '脚手架级微服务租房平台Q&A.md', 'filetype':
'text/markdown', 'last_modified': '2025-08-29T10:56:36', 'category': 'Title',
'element_id': '3a0670f9bfd58576e430ef11def41593'}
6
7 page_content='为什么做这个项目？' metadata={'source': '../Docs/Markdown/脚手架级微
服务租房平台Q&A.md', 'category_depth': 2, 'emphasized_text_contents': ['为什么做这
个项目？'], 'emphasized_text_tags': ['b'], 'languages': ['zho'],
'file_directory': '../Docs/Markdown', 'filename': '脚手架级微服务租房平台Q&A.md',
'filetype': 'text/markdown', 'last_modified': '2025-08-29T10:56:36',
'parent_id': '3a0670f9bfd58576e430ef11def41593', 'category': 'Title',
'element_id': 'fcb08b2a85942455eecebb9467ffca4c'}
8
9 page_content='回答1：（出于兴趣爱好开发）' metadata={'source': '../Docs/Markdown/脚
手架级微服务租房平台Q&A.md', 'emphasized_text_contents': ['回答1：（出于兴趣爱好开
发）'], 'emphasized_text_tags': ['b'], 'languages': ['zho'], 'file_directory':
'../Docs/Markdown', 'filename': '脚手架级微服务租房平台Q&A.md', 'filetype':
'text/markdown', 'last_modified': '2025-08-29T10:56:36', 'parent_id':
'fcb08b2a85942455eecebb9467ffca4c', 'category': 'UncategorizedText',
'element_id': 'a6fc0b5a457d21234bf1c4a6ae0a18db'}
可以看见文档被分成了 441 个。根据什么规则拆分呢？答案是根据类型拆分。
在元数据中，有一个表示类型的字段： 。这些类型都是现代文档解析库（如
category
Unstructured.io）中用于分类 Markdown（MD）文档或从其他格式（如 PDF, Word, HTML）解析后
转换为 Markdown 的常见元素类型。

让我们把当前文档包含的所有类型拿出来看看：
代码块
1 print(set(document.metadata["category"] for document in data))
结果如下：
代码块
1 {'Image', 'Title', 'ListItem', 'Table', 'NarrativeText', 'UncategorizedText'}
含义如下：
• Image：图像。使用   语法插入的图片。
![alt text](image-url)
• Title：标题。这里包含了一级、二级、三级等标题。
• ListItem：列表项。以  ,  ,   开头的无序列表项，或以  ,   等开头的有序列表项。
- * + 1. 2.
• Table：表格。使用   和   语法创建的表格。
| -
• NarrativeText：叙述性文本。一个或多个连续的段落。
• UncategorizedText：未分类文本。通常是：表格中的脚注或注释、图片下方的简短说明、项目
符号中非常简短的词组、文档页眉/页脚中的日期或页码等元数据。
这里的标题都是用 Title 表示，那么如何区分到底是几级标题呢？
首先，每个文档对象的元数据中都包含一个 用来识别其唯一性，除此之外，还有一个
element_id
元素，用来表示其归属于那个文档对象下，可以用来表示层级关系。如示例中的前三个
parent_id
文档对象，就有明显的层级关系：
代码块
1 page_content='通用问题' metadata={
2 'category': 'Title',
3 'element_id': '3a0670f9bfd58576e430ef11def41593'
4 }
5
6 page_content='为什么做这个项目？' metadata={
7 'parent_id': '3a0670f9bfd58576e430ef11def41593',
8 'category': 'Title',
9 'element_id': 'fcb08b2a85942455eecebb9467ffca4c'
10 }
11
12 page_content='回答1：（出于兴趣爱好开发）' metadata={
13 'parent_id': 'fcb08b2a85942455eecebb9467ffca4c',

14 'category': 'UncategorizedText',
15 'element_id': 'a6fc0b5a457d21234bf1c4a6ae0a18db'
16 }
经过这种设计，就可以还原出整个 MD 文档的内容了。
对于 LangChain 来说，能加载的文档类型远不止这些，它还能加载网页、一些云提供商文件、社交媒
体平台文档等，更多文档加载器见这里。
6. 文本分割器（Text splitters）
6.1 概念
我们已经知道可以通过文档加载器完成各种数据源的加载，将其转换为文档对象 。那么接
Document
下来要做的就是文档拆分。
文档拆分通常是将大文本分解为更小的、易于管理的块。这对于索引数据并将其传递到模型中都很有
用。因为，大块更难搜索并且不适合模型的有限上下文窗口。拆分可以提高搜索结果的粒度，从而可
以更精确地将查询与相关文档部分进行匹配。
LangChain 的文本分割器便能将大型文档分解为更小的块。如下图所示：

6.2 根据文档长度与文档语义拆分
我们可以直接根据文档的长度拆分文档，是最简单且有效的方法。可确保每个块不超过指定的大小限
制。对于长度拆分，其实也分为两种： 基于字符长度拆分 和 基于 长度拆分 。
Token
6.2.1 基于字符长度拆分
根据给定的字符序列进行拆分，拆分的块长度则按字符数来衡量。
代码块
1 from langchain_community.document_loaders import UnstructuredMarkdownLoader
2 from langchain_text_splitters import CharacterTextSplitter
3
4 markdown_path = "../Docs/Markdown/脚手架级微服务租房平台Q&A.md"
5 # single 模式加载后，默认只有一个 Document 对象
6 loader = UnstructuredMarkdownLoader(markdown_path)
7 data = loader.load()
8
9 # 文本分割器
10 text_splitter = CharacterTextSplitter(
11 separator="\n\n", # 选择分隔符：它有一个默认的分隔符优先级列表，通常是：
["\n\n", "\n", " ", ""]。它会按顺序尝试这些分隔符
12 chunk_size=100, # 设定目标：目标块大小
13 chunk_overlap=20, # 设定目标：块之间的重叠大小
14 length_function=len, # 使用测量长度的函数
15 is_separator_regex=False, # 分隔符是正则表达式吗
16 )
17
18 # 分割文档，返回被分割的文档列表
19 texts = text_splitter.split_documents(data)
20 # 打印前10个被分割出来的文档
21 for document in texts[:10]:
22 print("*" * 30)
23 print(f"{document}\n")

关于 所有初始化参数见这里。
CharacterTextSplitter
打印结果：
代码块
1 Created a chunk of size 128, which is longer than the specified 100
2 Created a chunk of size 133, which is longer than the specified 100
3 Created a chunk of size 108, which is longer than the specified 100
4 Created a chunk of size 104, which is longer than the specified 100
5 Created a chunk of size 127, which is longer than the specified 100
6 Created a chunk of size 112, which is longer than the specified 100
7 Created a chunk of size 121, which is longer than the specified 100
8 Created a chunk of size 137, which is longer than the specified 100
9 Created a chunk of size 133, which is longer than the specified 100
10 Created a chunk of size 108, which is longer than the specified 100
11 Created a chunk of size 1171, which is longer than the specified 100
12 Created a chunk of size 1171, which is longer than the specified 100
13 Created a chunk of size 112, which is longer than the specified 100
14 Created a chunk of size 128, which is longer than the specified 100
15 Created a chunk of size 102, which is longer than the specified 100
16 Created a chunk of size 155, which is longer than the specified 100
17 Created a chunk of size 121, which is longer than the specified 100
18 Created a chunk of size 284, which is longer than the specified 100
19 Created a chunk of size 107, which is longer than the specified 100
20 Created a chunk of size 206, which is longer than the specified 100
21 Created a chunk of size 161, which is longer than the specified 100
22 Created a chunk of size 115, which is longer than the specified 100
23 Created a chunk of size 159, which is longer than the specified 100
24 Created a chunk of size 125, which is longer than the specified 100
25 Created a chunk of size 150, which is longer than the specified 100
26 Created a chunk of size 102, which is longer than the specified 100
27 Created a chunk of size 106, which is longer than the specified 100
28 Created a chunk of size 107, which is longer than the specified 100
29 Created a chunk of size 238, which is longer than the specified 100
30 Created a chunk of size 113, which is longer than the specified 100
31 Created a chunk of size 102, which is longer than the specified 100
32 Created a chunk of size 206, which is longer than the specified 100
33 Created a chunk of size 282, which is longer than the specified 100
34 ******************************
35 page_content='通用问题
36
37 为什么做这个项目？
38
39 回答1：（出于兴趣爱好开发）' metadata={'source': '../Docs/Markdown/脚手架级微服务租房
平台Q&A.md'}

40
41 ******************************
42 page_content='大学期间，我和同学在外合租过一段时间，使用了一些租房平台，于是我有个想法，
自己能不能开发一个租房平台，可以让我将理论知识与实践相结合。我希望通过实际项目来加深对
Java 编程语言和相关技术的理解。于是我便查找了一些资料，看了一些开源项目，进行了一些改进。'
metadata={'source': '../Docs/Markdown/脚手架级微服务租房平台Q&A.md'}
43
44 ******************************
45 page_content='回答2：（开源项目的解释）' metadata={'source': '../Docs/Markdown/脚手
架级微服务租房平台Q&A.md'}
46
47 ******************************
48 page_content='这个项目其实是在 github 上找到的一个开源项目，主要是可以支持一些常规的聊
天项目，我对于聊天如何实现的比较感兴趣。顺便也想锻炼一下自己工程代码能力，在网上就找到了这
个开源项目和项目的一些比较完善的文档和介绍，再加上找了一个业务场景：租房。所以就确定了这个
项目。' metadata={'source': '../Docs/Markdown/脚手架级微服务租房平台Q&A.md'}
49
50 ******************************
51 page_content='回答3：（学校课设的解释）
52
53 这是学校的课设题目，然后在课设项目的基础上，查找了一些资料，进行了一些改进。
54
55 这个项目为啥和上个(之前)同学的项目一样？
56
57 回答1：（开源项目的回答）' metadata={'source': '../Docs/Markdown/脚手架级微服务租房平
台Q&A.md'}
58
59 ******************************
60 page_content='因为这个项目本身就是开源项目，网上一搜都是好多都是这个项目的博客分析，写得
优质的主要就是那几篇，可能我们参考了一样的博客讲解梳理框架吧。抽奖系统虽然听起来简单，但实
际上涉及到数据存储、状态转换、异常处理等多个技术点。' metadata={'source':
'../Docs/Markdown/脚手架级微服务租房平台Q&A.md'}
61
62 ******************************
63 page_content='回答2：（学校课设的回答）
64
65 这个问题面试官可能会直接的指出：上个同学和你学校不同，但是跟你的项目完全一样。' metadata=
{'source': '../Docs/Markdown/脚手架级微服务租房平台Q&A.md'}
66
67 ******************************
68 page_content='这个项目确实是我们学校的开源项目，但是我在实现的时候也在网上找了跟课设要求
类似的开源项目，而且发现网上针对这个类型的项目文档还听完善的，可能其他的同学也是在网上跟我
一样找到了类似的博客或开源项目进行借鉴的吧。' metadata={'source': '../Docs/Markdown/
脚手架级微服务租房平台Q&A.md'}
69
70 ******************************

71 page_content='（注：以下内容需要同学自己思考项目扩展，如不清楚，不建议参照话术，容易给自
己挖坑。说到上面的内容就可以了。不要使用课上讲解的扩展，大家都说一样的也不行）' metadata=
{'source': '../Docs/Markdown/脚手架级微服务租房平台Q&A.md'}
72
73 ******************************
74 page_content='但这个项目除了常规流程，我还进行了（功能扩展/性能优化/代码优化）.....(并
解释一下扩展点)' metadata={'source': '../Docs/Markdown/脚手架级微服务租房平台
Q&A.md'}
，这
Created a chunk of size xxx, which is longer than the specified 100
表示分割时，超出了我们设定的 目标块大小。在这里要说明，这是一个在使用
chunk_size=100
LangChain 的文本分割器时非常常见的问题。看到这个信息，不要担心，这不是错误，而是预期的行
为。
原因是为了保持语义的完整性！当文本分割器用尽所有指定的分隔符都无法将一段文本分割到你的目
标大小 以下时，它会选择保留整个文本块，而不是强行将其截断为无意义的片段，因
chunk_size
此我们会看到这个提示信息。因此我们可以看到，被分割出来的段落，基本上都是语义完整的一段
话。
那么分割逻辑到底是什么，可以支持保持语义完整性？
• 尝试分割：
◦ 首先，它尝试用 （我们设置的是 双换行，这通常代表段落之间）来分
separator "\n\n"
割文本。如果分割后的任何一个段落仍然大于 ，它会继续下一步。
chunk_size
• 如果仍然有单个单词或字符串的长度超过了  ，分割器就陷入了两难境地：
100
◦ 选项A：强行把这个长字符串在任意位置（比如第100个字符处）截断。但这会破坏单词、URL
或数据的完整性，导致生成无意义的片段（例如，把 "Christopher" 截断成 "Christop" 和
"her"），严重影响后续的嵌入或语言模型处理效果。
◦ 选项B：保留这个完整的、超长的字符串作为一个块，并记录一条信息告知用戶。
那么很明显，这里它选择了B选项。因此，我们看到了
Created a chunk of size 128,
 这条日志信息。
which is longer than the specified 100
如何应对和处理？增大  ：如果我们的大部分块都超长，可能是   设置得
chunk_size chunk_size
太小了。尝试适当增大它。
因此，根据打印的日志，可以发现，被拆分的文本大都徘徊的100~200之间，因此，可以将
设置为200：
chunk_size
代码块
1 text_splitter = CharacterTextSplitter(
2 separator="\n\n",
3 chunk_size=200,
4 chunk_overlap=20,

5 length_function=len,
6 is_separator_regex=False,
7 )
运行后，可以看到超出的分割已经很少了。
6.2.2 基于 Token 长度拆分
之前我们讲过，LLM 大模型实际上并不是直接接收字符串，而是需要先做 token 切分编码。这里我们
可以借助 【 分词器】来进行 token 的切分编码。
tiktoken
先看个例子：给定一个文本字符串 ，使用 分词器进行切分编
"my name is LiHua!" tiktoken
码，会得到什么结果：
代码块
1 import tiktoken
2
3 # 定于cl100k_base编码方式的分词器
4 enc = tiktoken.get_encoding("cl100k_base")
5
6 # 进行切分编码
7 enc_output = enc.encode("my name is LiHua!")
8
9 # 打印结果
10 print(f"编码后的token：{str(enc_output)}")
11 for token in enc_output:
12 print(f"将token: {str(token)} 变成文本:
{str(enc.decode_single_token_bytes(token))}")
解释： 是 分词器中的一种编码方式。 、 等
cl100k_base tiktoken gpt-4 gpt-3.5-turbo
都采用这种切分编码方式。
可以看到采用切分编码 ，拆解后的文本字符串为
cl100k_base ["my", "name", "is", "
。token 编码表示为
Li", "H", "ua", "!"] [2465, 836, 374, 14851, 39, 4381,
。
0]
以上跟大家介绍了基于 Token 拆分文本的基本方式，主要就是了解设定了某种编码格式的
分词器可以进行文本拆分。那么接下来，我们就可以使用根据 编码方式
tiktoken cl100k_base
的   分词器来拆分文档。这对于 OpenAI 模型来说，会更准确。
tiktoken
在LangChain中，我们可以使用 分割器
CharacterTextSplitter
的 方法来定义根据 分词器拆分文本的分割器，代码如
.from_tiktoken_encoder() tiktoken
下所示：

代1 码块fr om langchain_community.document_loaders import UnstructuredMarkdownLoader
2 from langchain_text_splitters import CharacterTextSplitter
3
4 markdown_path = "../Docs/Markdown/脚手架级微服务租房平台Q&A.md"
5 # single 模式加载后，默认只有一个 Document 对象
6 loader = UnstructuredMarkdownLoader(markdown_path)
7 data = loader.load()
8
9 # 生成分割器
10 text_splitter = CharacterTextSplitter.from_tiktoken_encoder(
11 encoding_name="cl100k_base", chunk_size=200, chunk_overlap=50
12 )
13 # 分割文档
14 texts = text_splitter.split_documents(data)
15 # 打印前10个被分割出来的文档
16 for document in texts[:10]:
17 print("*" * 30)
18 print(f"{document}\n")
结果如下：
代码块
1 Created a chunk of size 916, which is longer than the specified 200
2 Created a chunk of size 916, which is longer than the specified 200
3 Created a chunk of size 260, which is longer than the specified 200
4 ******************************
5 page_content='通用问题
6
7 为什么做这个项目？
8
9 回答1：（出于兴趣爱好开发）
10
11 大学期间，我和同学在外合租过一段时间，使用了一些租房平台，于是我有个想法，自己能不能开发一
个租房平台，可以让我将理论知识与实践相结合。我希望通过实际项目来加深对 Java 编程语言和相关
技术的理解。于是我便查找了一些资料，看了一些开源项目，进行了一些改进。
12
13 回答2：（开源项目的解释）' metadata={'source': '../Docs/Markdown/脚手架级微服务租房平
台Q&A.md'}
14
15 ******************************
16 page_content='回答2：（开源项目的解释）
17
18 这个项目其实是在 github 上找到的一个开源项目，主要是可以支持一些常规的聊天项目，我对于聊
天如何实现的比较感兴趣。顺便也想锻炼一下自己工程代码能力，在网上就找到了这个开源项目和项目
的一些比较完善的文档和介绍，再加上找了一个业务场景：租房。所以就确定了这个项目。

19
20 回答3：（学校课设的解释）' metadata={'source': '../Docs/Markdown/脚手架级微服务租房平
台Q&A.md'}
21
22 ******************************
23 page_content='回答3：（学校课设的解释）
24
25 这是学校的课设题目，然后在课设项目的基础上，查找了一些资料，进行了一些改进。
26
27 这个项目为啥和上个(之前)同学的项目一样？
28
29 回答1：（开源项目的回答）' metadata={'source': '../Docs/Markdown/脚手架级微服务租房平
台Q&A.md'}
30
31 ******************************
32 page_content='这个项目为啥和上个(之前)同学的项目一样？
33
34 回答1：（开源项目的回答）
35
36 因为这个项目本身就是开源项目，网上一搜都是好多都是这个项目的博客分析，写得优质的主要就是那
几篇，可能我们参考了一样的博客讲解梳理框架吧。抽奖系统虽然听起来简单，但实际上涉及到数据存
储、状态转换、异常处理等多个技术点。
37
38 回答2：（学校课设的回答）' metadata={'source': '../Docs/Markdown/脚手架级微服务租房平
台Q&A.md'}
39
40 ******************************
41 page_content='回答2：（学校课设的回答）
42
43 这个问题面试官可能会直接的指出：上个同学和你学校不同，但是跟你的项目完全一样。
44
45 这个项目确实是我们学校的开源项目，但是我在实现的时候也在网上找了跟课设要求类似的开源项目，
而且发现网上针对这个类型的项目文档还听完善的，可能其他的同学也是在网上跟我一样找到了类似的
博客或开源项目进行借鉴的吧。' metadata={'source': '../Docs/Markdown/脚手架级微服务租
房平台Q&A.md'}
46
47 ******************************
48 page_content='（注：以下内容需要同学自己思考项目扩展，如不清楚，不建议参照话术，容易给自
己挖坑。说到上面的内容就可以了。不要使用课上讲解的扩展，大家都说一样的也不行）
49
50 但这个项目除了常规流程，我还进行了（功能扩展/性能优化/代码优化）.....(并解释一下扩展点)
51
52 可以参考的回答思路：即使面试官的指控让你感到不舒服，也要尽量保持冷静和专注。无论情况如何，
保持专业和礼貌的态度是非常重要的。' metadata={'source': '../Docs/Markdown/脚手架级微
服务租房平台Q&A.md'}
53
54 ******************************

55 page_content='诚实面对： 如果确实使用了代码而没有适当的引用或修改，那么诚实地承认这一
点，可以说是从github上找的开源项目，并解释你当时的想法和决策过程（参考答案举例）。同时，如
果抄袭代码被面试官发现，要及时表明你已经认识到了这个问题，并在未来的工作中会更加注意。
56
57 展示学习过程： 如果是面试官发现项目确实有抄袭现象，也可以说项目原本的一些问题，你进行了优
化解决，体现自己有过思考，并解释问题与优化思路。' metadata={'source':
'../Docs/Markdown/脚手架级微服务租房平台Q&A.md'}
58
59 ******************************
60 page_content='请求反馈： 询问面试官对你的项目有什么具体的反馈或建议，这表明你愿意学习和
改进。
61
62 介绍一下这个项目。
63
64 回答一：（精简版）
65
66 基于脚手架的微服务在线租房系统，业务模型对标了贝壳、安居客、闲鱼等流行应用。无论是交互体
验、架构设计，还是数据模拟都达到真实的工业水准设计。' metadata={'source':
'../Docs/Markdown/脚手架级微服务租房平台Q&A.md'}
67
68 ******************************
69 page_content='本项目是一个 Java 综合项目，涵盖【前端】与【后端】开发，业务上包含房源管
理、租戶和房东沟通、房源上下架等整套业务流程。核心技术包括微服务、分布式集群、高并发、分布
式对象存储、高速缓存、消息队列、即时通信、解决方案设计。' metadata={'source':
'../Docs/Markdown/脚手架级微服务租房平台Q&A.md'}
70
71 ******************************
72 page_content='后端采用 Spring Cloud 微服务架构，引入脚手架，将系统拆分为五个微服务。服
务间借助 OpenFeign 实现相互调用。同时，项目集成了 Nginx、Nacos、MySQL 等 10 多个主流
Java 后端组件，技术栈丰富且前沿。
73
74 前端包含 Web管理端 和 微信小程序端，基于 Vue 3 框架，使用 Vite 构建工具，以
JavaScript 为主要语言，利用 Axios 高效交互数据，并引入 Element-Plus 组件库，让系统界面
既实用又美观。C 端使用了微信小程序作为面向移动端应用入口。' metadata={'source':
'../Docs/Markdown/脚手架级微服务租房平台Q&A.md'}
75
同样，为了保持语义的完整性，文本分割器无法将一段文本分割到你的目标大小 以
chunk_size
下，它会选择保留整个文本块。
6.2.3 硬性约束长度拆分
如果我们就想要求任何块都不能超过指定大小，可以使用
RecursiveCharacterTextSplitter
类 或

方法，它会严格遵守对块
RecursiveCharacterTextSplitter.from_tiktoken_encoder
大小的硬约束。下面展示一下用法：
代码块
1 from langchain_community.document_loaders import UnstructuredMarkdownLoader
2 from langchain_text_splitters import RecursiveCharacterTextSplitter
3
4 markdown_path = "../Docs/Markdown/脚手架级微服务租房平台Q&A.md"
5 # single 模式加载后，默认只有一个 Document 对象
6 loader = UnstructuredMarkdownLoader(markdown_path)
7 data = loader.load()
8
9 # 生成分割器
10 text_splitter = RecursiveCharacterTextSplitter.from_tiktoken_encoder(
11 encoding_name="cl100k_base",
12 chunk_size=100,
13 chunk_overlap=0,
14 )
15
16 # 分割文档
17 texts = text_splitter.split_documents(data)
18 # 打印前10个被分割出来的文档
19 for document in texts[:10]:
20 print("*" * 30)
21 print(f"{document}\n")
结果如下，严格不超出指定块大小：
代码块
1 ******************************
2 page_content='通用问题
3
4 为什么做这个项目？
5
6 回答1：（出于兴趣爱好开发）' metadata={'source': '../Docs/Markdown/脚手架级微服务租房
平台Q&A.md'}
7
8 ******************************
9 page_content='大学期间，我和同学在外合租过一段时间，使用了一些租房平台，于是我有个想法，
自己能不能开发一个租房平台，可以让我将理论知识与实践相结合。我希望通过实际项目来加深对
Java' metadata={'source': '../Docs/Markdown/脚手架级微服务租房平台Q&A.md'}
10
11 ******************************

12 page_content='编程语言和相关技术的理解。于是我便查找了一些资料，看了一些开源项目，进行了
一些改进。' metadata={'source': '../Docs/Markdown/脚手架级微服务租房平台Q&A.md'}
13
14 ******************************
15 page_content='回答2：（开源项目的解释）' metadata={'source': '../Docs/Markdown/脚手
架级微服务租房平台Q&A.md'}
16
17 ******************************
18 page_content='这个项目其实是在 github' metadata={'source': '../Docs/Markdown/脚手
架级微服务租房平台Q&A.md'}
19
20 ******************************
21 page_content='上找到的一个开源项目，主要是可以支持一些常规的聊天项目，我对于聊天如何实现
的比较感兴趣。顺便也想锻炼一下自己工程代码能力，在网上就找到了这个开源项目和项目的一些比较
完善的' metadata={'source': '../Docs/Markdown/脚手架级微服务租房平台Q&A.md'}
22
23 ******************************
24 page_content='文档和介绍，再加上找了一个业务场景：租房。所以就确定了这个项目。'
metadata={'source': '../Docs/Markdown/脚手架级微服务租房平台Q&A.md'}
25
26 ******************************
27 page_content='回答3：（学校课设的解释）
28
29 这是学校的课设题目，然后在课设项目的基础上，查找了一些资料，进行了一些改进。
30
31 这个项目为啥和上个(之前)同学的项目一样？
32
33 回答1：（开源项目的回答）' metadata={'source': '../Docs/Markdown/脚手架级微服务租房平
台Q&A.md'}
34
35 ******************************
36 page_content='因为这个项目本身就是开源项目，网上一搜都是好多都是这个项目的博客分析，写得
优质的主要就是那几篇，可能我们参考了一样的博客讲解梳理框架吧。抽奖系统虽然听起来简单，但
实' metadata={'source': '../Docs/Markdown/脚手架级微服务租房平台Q&A.md'}
37
38 ******************************
39 page_content='际上涉及到数据存储、状态转换、异常处理等多个技术点。' metadata=
{'source': '../Docs/Markdown/脚手架级微服务租房平台Q&A.md'}
40
但这样，其实是剥夺了一些保证语义完整性的能力，可以看到某些含义相似的内容，被强制分开。
除此之外，还要再说明的是，文本内容是中文时，使用默认分隔符列表  ， ，
[“\n\n” “\n” “
，  拆分文本可能会导致一个词组被拆分成两个字，导致语义失效。若要将词组放在一起，可以
” “”]
覆盖分隔符列表以包含其他标点符号，例如中文的逗号 ， 、句号 。 或其他中文符号，如下所示：

代1 码块text_splitter = RecursiveCharacterTextSplitter(
2 separators=[
3 "\n\n",
4 "\n",
5 " ",
6 "。",
7 ",",
8 "",
9 ],
10 # Existing args
11 )
这样在分割时将递归用 来尝试分割文本：
separators
• 首先，它尝试用  （双换行，通常代表段落）来分割，如果分割后的任何一个段落仍然大
"\n\n"
于 ，它会继续下一步。
chunk_size
• 接着，它尝试用  （单换行，通常代表行之间）来分割那些仍然过大的段落。
"\n"
• 然后，它尝试用  （空格，单词之间）来分割。
" "
• ....
6.3 特殊文档结构拆分
若对于代码等特殊文本，可以尝试使用   提供的不同的分割器（如
Language
、 等）效果会更好，它会理解代码的
PythonCodeTextSplitter HTMLHeaderTextSplitter
语法结构。
这里了解下常见的拆分原则即可：
• Markdown：根据标头拆分（例如，#、##、###）
• HTML：使用标签拆分
• JSON： 按对象或数组元素拆分
• Code 代码 ：按函数、类或逻辑块拆分
这里我们以 Python 代码举例，其他的使用姿势可以参考官网接口，实际上用法与我们上面讲解的类
似。
代码块
1 from langchain_text_splitters import PythonCodeTextSplitter
2
3 # 字符串文档
4 PYTHON_CODE = """
5 def hello_world():

6 print("Hello, World!")
7
8 def hello_python():
9 print("Hello, Python!")
10 """
11 python_splitter = PythonCodeTextSplitter(chunk_size=50, chunk_overlap=0)
12 python_docs = python_splitter.create_documents([PYTHON_CODE])
13 for document in python_docs[:2]:
14 print("*" * 30)
15 print(f"{document}\n")
结果如下：
代码块
1 ******************************
2 page_content='def hello_world():
3 print("Hello, World!")'
4
5 ******************************
6 page_content='def hello_python():
7 print("Hello, Python!")'
7. 文本向量
7.1 嵌入与嵌入模型（Embedding and Embedding Models）
计算机天生擅长处理数字，但不理解文字、图片的含义。嵌入（Embedding）的核心思想就是将人类
世界的符号（如单词、句子、产品、用戶、图片）转换为计算机能够理解的数值形式（即向量，本质
上是一个数字列表），并且要求这种转换能够保留原始符号的语义和关系。
我们可以把它想象成一个翻译过程，把人类语言“翻译”成计算机的“数学语言”。

说明：我们之前一直用的大语言模型是生成式模型。它理解输入并生成新的文本（回答问题、写文
章）。它内部实际上也使用嵌入技术来理解输入，但最终目标是“创造”。
而嵌入模型（Embedding Models）是表示型模型。它的目标不是生成文本，而是为输入的文本创建
一个最佳的、富含语义的数值表示（向量）。如 OpenAI 的 "text-embedding-3-large" 嵌入模型；
Google 的 "gemini-embedding-001" 嵌入模型；阿里的 "Qwen3-Embedding-8B" 嵌入模型等。
7.1.1 什么是向量？
首先我们要知道，嵌入的结果是就是一个向量，它本质上是一个数字列表（一维数组）。例如：
。对于向量来说，有两个关键概念需要了解：
[0.023, 0.487, -0.129, ..., 0.325]
向量维度
嵌入结果得到的列表长度是固定的，称为向量的“维度”。例如，OpenAI 的
text-embedding-
 模型会生成一个 1536 维的向量， 模型会生成一个 3072 维
ada-002 text-embedding-3-large
的向量。
维度越高，通常能捕捉更细微的语义信息，但也需要更多的计算和存储资源。
向量空间
想象一个无限延伸的、拥有无数个维度的宇宙，这个宇宙就是一个向量空间。这有点抽象，可以想象
一下：
• 在三维世界里，一个点可以用   坐标表示，例如  。
(x, y, z) (2, 5, -1)
• 在机器学习的高维向量空间中，一个点可能是  ，一
(0.1, 0.7, -0.2, 0.4, ..., 0.02)
个有几百或几千个数字的坐标。
在这个空间里，每个点（即每个向量）都能代表一个概念。例如在嵌入模型中，一个点可以代表一个
单词、一句话、一张图片、一个用戶、一部电影等。

到这里，向量空间的威力就能体现出来：我们可以用数学来度量语义。可以通过计算两个向量之间
的“距离”或“相似度”来实现这一点。
• 欧氏距离（Euclidean Distance）：就是我们高中几何学的两点之间的直线距离。距离越短，相似
度越高。
• 余弦相似度（Cosine Similarity）：它忽略向量的绝对长度（大小），只关注两个向量在方向上的
差异。在文本和语义的世界里，“方向”代表“含义”，而“长度”往往只代表“文本的长
度”或“词汇的多少”。换句话说，余弦相似度关注的是 你们是否指向同一个方向 / 你们是
“ ” “
否代表同一个含义
”
因此，在捕捉语义上的相似性上，余弦相似度是更常用的度量方式。
我们又能反推出，由于使用向量来绘制向量空间，而向量是有维度的，维度越高，则更能捕捉极其细
微和复杂的语义差别（比如“高兴”和“喜悦”的区别）。
这能干什么？这能解决一个传统数据库（如MySQL）不擅长的问题：基于内容的相似性搜索，而不是
基于精确匹配的查询。
7.1.2 嵌入模型应用场景
对于嵌入模型，实际上在示例选择器部分，我们已经使用过。当时使用的场景就是可以根据语义相似
性完成示例的筛选。
根据嵌入的特性，由此延伸出了许多嵌入模型在 AI 应用的使用场景：
• 语义搜索（Semantic Search）
传统搜索依赖关键词匹配（搜 苹果 ，只能找到包含 苹果 这个词的文档）。
“ ” “ ”

语义搜索则能将查询（如 一种红色的水果 ）和文档库中的所有文档都转换为向量。然后计算查询
“ ”
向量与所有文档向量的相似度，返回最相似的文档。这样即使文档里没有 红色 和 水果 这些
“ ” “ ”
词，但只要它是关于 苹果 的，就能被找到。
“ ”
如下图为我们展示了借助嵌入模型进行文档搜索的过程：
1. 为多文档生成其各自的向量，
2. 为搜索查询语句生成向量，
3. 衡量查询向量与每个文档向量之间的相似性，得到相似度最高的文档。
• 检索增强生成（Retrieval-Augmented Generation, RAG）
这是当前大语言模型应用的核心模式。当用戶向 LLM 提问时，系统首先使用嵌入模型在知识库（如公
司内部文档）中进行语义搜索，找到最相关的内容，然后将这些内容和问题一起交给 LLM 来生成答
案。这极大地提高了答案的准确性和时效性。
• 推荐系统（Recommendation Systems）
将用戶（根据其历史行为、偏好）和物品（商品、电影、新闻）都转换为向量。喜欢相似物品的用
戶，其向量会接近；相似的物品，其向量也会接近。通过计算用戶和物品向量的相似度，就可以进行
精准推荐。
• 异常检测（Anomaly Detection）
正常数据的向量通常会聚集在一起。如果一个新数据的向量远离大多数向量的聚集区，它就可能是一
个异常点（如垃圾邮件、欺诈交易）。
7.2 Embeddings 嵌入模型类

在 LangChain 中，有很多的嵌入模型提供方，使用不同的模型提供方，需要安装为其各自包，例如：
• OpenAI：
pip install -U langchain-openai
• Ollama：
pip install -U langchain-ollama
• Google Gemini：
pip install -U langchain-google-genai
• 更多见这里。
7.2.1  定义嵌入模型
在这里我们选择 OpenAI 来进行后续操作。定义 OpenAI 下的嵌入模型使用：
，官方接口介绍见这
class langchain_openai.embeddings.base.OpenAIEmbeddings
里。
定义 OpenAIEmbeddings 嵌入模型类与定义聊天模型类似，如下所示：
代码块
1 from langchain_openai import OpenAIEmbeddings
2 embeddings = OpenAIEmbeddings(
3 model="text-embedding-3-large", # 是 OpenAI 2024年发布的最新嵌入模型，生成3072维
的高质量向量
4 )
在 LangChain 框架中基础 Embeddings 类（ 继承了它）设计了两个核心方法
OpenAIEmbeddings
来处理文本嵌入。
• ： 用于处理文档 。它的输入是多个文本。例如要将一个
.embed_documents() Documents
知识库里的所有段落都转换成向量后存入数据库，就会使用这个方法。
◦ 它返回一个【二维列表】 。外层列表的每个元素对应一个输入文档，
List[List[float]]
内层列表则是该文档的向量表示。
• ： 用于处理查询 。它的输入是单个文本（一个字符串，str）。例
.embed_query() Query
如，当用戶提出一个问题时，需要将这个问题转换成向量，以便在数据库中搜索相似的文档段落，
就会使用这个方法。
◦ 它返回一个【一维列表】，里面是浮点数（ ），代表单个查询文本的向量。
List[float]
其实分别对应下图中文档与查询的向量生成：

之所以设计成两个方法，是因为某些嵌入模型提供商（如 OpenAI、Cohere 等）会针对 被搜索的文
“
档 和 搜索查询本身 采用不同的优化策略和模型。即使底层是同一个模型，也可能对两者进行不
” “ ”
同的预处理（例如添加不同的指令前缀），以获得更好的搜索效果。
7.2.2 嵌入文档列表
 的语义是 “索引”。它的目的是预处理大量文本，为它们创建向量表示，以便
embed_documents
后续被搜索。这一般是一个离线、批量处理的过程。代码如下：
代码块
1 from langchain_openai import OpenAIEmbeddings
2 from langchain_community.document_loaders import UnstructuredMarkdownLoader
3 from langchain_text_splitters import CharacterTextSplitter
4
5 markdown_path = "../Docs/Markdown/脚手架级微服务租房平台Q&A.md"
6 # single 模式加载后，默认只有一个 Document 对象
7 loader = UnstructuredMarkdownLoader(markdown_path)
8 data = loader.load()
9
10 # 生成分割器
11 text_splitter = CharacterTextSplitter.from_tiktoken_encoder(
12 encoding_name="cl100k_base", chunk_size=200, chunk_overlap=50
13 )
14 # 分割文档
15 documents = text_splitter.split_documents(data)
16
17 # 定义嵌入模型

18 embeddings = OpenAIEmbeddings(model="text-embedding-3-large")
19
20 # 嵌入文档列表，生成向量列表
21 # 注意这里需要提取文档内容为字符串列表，才能传递给嵌入模型
22 texts = [doc.page_content for doc in documents]
23 documents_vector = embeddings.embed_documents(texts)
24 print(f"文档数量为：{len(documents)}，生成了{len(documents_vector)}个向量的列表")
25 print(f"第一个文档向量维度：{len(documents_vector[0])}")
26 print(f"第二个文档向量维度：{len(documents_vector[1])}")
结果如下：
代码块
1 Created a chunk of size 916, which is longer than the specified 200
2 Created a chunk of size 916, which is longer than the specified 200
3 Created a chunk of size 260, which is longer than the specified 200
4 文档数量为：96，生成了96个向量的列表
5 第一个文档向量维度：3072
6 第二个文档向量维度：3072
可以看到 嵌入模型，生成的便是3072维的高质量向量。共存在96个文
text-embedding-3-large
档，因此生成了96个向量的列表。
7.2.3 嵌入单个查询
 的语义是 “搜索”。它的目的是在用戶发起请求时，实时地将一个问题或指令转换
embed_query
为向量，用于在已索引的文档向量中进行检索。这是一个在线、实时、按需处理的过程。
为单个查询生成向量的代码如下：
代码块
1 from langchain_openai import OpenAIEmbeddings
2
3 # 定义嵌入模型
4 embeddings = OpenAIEmbeddings(model="text-embedding-3-large")
5 # 嵌入单个查询
6 query_vector = embeddings.embed_query("项目中遇到了哪些挑战？如何解决？")
7 print(f"向量维度：{len(query_vector)}")
8 print(f"向量前五个数值为：{query_vector[:5]}")
结果如下：

代1 码块向 量维度：3072
2 向量前五个数值为：[-0.01306734886020422, -0.004718094132840633,
-0.005446741823107004, -0.014186487533152103, 0.004673811607062817]
7.3 向量存储（Vector Stores）
7.3.1 向量数据库介绍
在 LangChain 中，实际并不需要我们直接手动调用嵌入模型去生成向量，然后手动去比较向量。在我
们之前提供的 RAG 知识地图中，存在一个 Vector Stores 向量存储，如下图所示：
实际上，向量是被管理在专门的向量存储介质中，如向量数据库。向量存储的核心任务是解决一个传
统数据库（如MySQL）不擅长的问题：基于内容的相似性搜索（Similarity Search），而不是基于精
确匹配的查询。

根据上图，我们想要从向量数据库中进行相似度搜索，首先就需要将向量存储管理起来。想象一下，
一篇文档若转换成一个有1536个浮点数的向量。一百万篇文档就是1536MB（约1.5GB）的纯向量数
据。这只是一个起点，现实中的数据集可能轻松达到千万甚至亿级。如何高效地存储和管理这些向
量？
向量数据库则提供了专门用于高效存储、管理和检索高维向量的能力。其核心就是 “高效地组织和检
索这些数据”。
常见的向量数据库核心机制如下：
• 专门的索引
这是向量数据库的灵魂。它们不会使用暴力搜索，而是会预先为所有向量构建一种特殊的索引结构。
常见的方法有近似最近邻（ANN）搜索：为了追求极致的速度，它愿意牺牲一点点精度。它不会保证
找到绝对最相似的向量（即最近邻），但能以极高的概率找到非常相似的向量。通过聚类、分层、压
缩等算法技术，将搜索范围从“整个数据库”缩小到“几个最可能的候选集”。
这就像在一个大图书馆里找书，你不是从 A 到 Z 遍历所有书架，而是先根据分类（文学、历史、科
学）找到大概区域，再在这个区域内仔细找，这样快得多。
• 向量相似度计算优化
向量数据库底层使用高度优化的库来进行向量运算。如 FAISS 向量数据库，它是 Facebook AI 研究院
开发的一种高效的相似性搜索和聚类的库。它能够快速处理大规模数据，并且支持在高维空间中进行
相似性搜索。
这些库充分利用了 CPU 的 SIMD 指令集和 GPU 的并行计算能力，让大规模的向量计算速度极快。
SIMD 指令集指 单指令多数据流 技术，是一种采用一个控制器来控制多个处理器，本质上非常类
“ ”
似一个向量处理器，可对控制器上的一组数据（又称“数据向量”） 同时分别执行相同的操作从而
实现空间上的并行。简单来说，就是一个指令能够同时处理多个数据。

• 数据管理功能
现代的向量数据库不仅有专门的索引，还提供了完整的数据管理功能：
• CRUD 操作：支持增删改查，可以动态地更新向量数据。
• 元数据过滤：这是非常关键的功能。除了向量本身，每篇文档还有元数据（Metadata），比如创
建时间、作者、类别等。向量数据库允许你先用元数据过滤（“找出xxxx年以后的、属于科技类
别的文档”），再在这个缩小的范围内进行向量相似度搜索，极大地提升了准确性和效率。
• 可扩展性与持久化：它们可以轻松地分布式部署，处理海量数据；同时保证数据持久化，不像纯
内存方案一样断电丢失。
• 集成方便：提供友好的API（如gRPC, RESTful），使得像 LangChain 这样的框架可以轻松地与
之集成，开发者无需关心底层细节。
因此，我们可以利用专门的向量数据库（如Chroma, Weaviate, Pinecone, Qdrant, Milvus等），通过
构建索引、优化计算流程、并提供丰富的过滤和管理功能，从而实现对海量高维向量数据进行快速、
可扩展的【相似性检索】的一套完整技术方案。
LangChain 框架则通过与这些向量数据库集成，让开发者无需手动处理向量生成、存储和比较的复杂
性，只需关注业务逻辑本身，极大地提高了开发效率和应用性能。
下面我们将演示如何使用 LangChain 提供的内存级存储，与集成的向量数据库。查看 LangChain 支持
的所有向量数据库点击这里。除了课件上的向量数据库使用，其他存储可查阅官方文档自行接入。
7.3.2 内存存储
我们将使用 LangChain 的 InMemoryVectorStore 来实现向量的内存存储。
7.3.2.1 基本操作
• 初始化
LangChain 中的大多数向量在初始化向量存储时接受嵌入模型作为参数。如下所示：

代码块
1 from langchain_openai import OpenAIEmbeddings
2 from langchain_core.vectorstores import InMemoryVectorStore
3
4 # 定义嵌入模型
5 embeddings = OpenAIEmbeddings(model="text-embedding-3-large")
6 # 内存存储初始化
7 vector_store = InMemoryVectorStore(embedding=embeddings)
• 添加文档
我们可以使用   方法，向内存存储中去添加文档。要注意的是，该方法会为添加的
add_documents
文档编排索引，索引列表随着该方法返回。
这也就是在前文中，我们一直在提的：当我们想对某文本进行【数据检索】时的两个步骤：
• 编制索引： 用于从源中摄取数据并为其编制索引。
• 检索和生成 ：接受用戶查询并从索引中检索相关数据，然后将其传递给模型。
对于第一步，我们终于要完成了。如下所示：
代码块
1 from langchain_community.document_loaders import UnstructuredMarkdownLoader
2 from langchain_text_splitters import CharacterTextSplitter
3
4 # 生成分割器
5 text_splitter = CharacterTextSplitter.from_tiktoken_encoder(
6 encoding_name="cl100k_base", chunk_size=200, chunk_overlap=50
7 )
8 # 加载文档
9 data = UnstructuredMarkdownLoader("../Docs/Markdown/脚手架级微服务租房平台
Q&A.md").load()
10 # 分割文档
11 documents = text_splitter.split_documents(data)
12 # 添加文档
13 ids = vector_store.add_documents(documents=documents)
14 print(f"共编排了{len(ids)}个文档索引")
15 print(f"前3个文档的索引是：{ids[:3]}")
结果输出：
代码块
1 共编排了96个文档索引

2 前3个文档的索引是：['3b22073d-8a8f-4f74-b99a-09d6343e7602', '3c2e05ea-1681-4d5e-
a31c-f01f2e0a0ac8', 'dcc3da40-b1bf-4ae3-9fd4-1f758b47af0f']
• 获取文档
使用 方法，通过索引列表获取对应的文档列表。如下所示：
get_by_ids
代码块
1 doc_3 = vector_store.get_by_ids(ids[:3])
2 print(f"{[doc.page_content for doc in doc_3]}")
结果如下：
代码块
1 [
2 '通用问题\n\n为什么做这个项目？\n\n回答1：（出于兴趣爱好开发）\n\n大学期间，我和同学
在外合租过一段时间，使用了一些租房平台，于是我有个想法，自己能不能开发一个租房平台，可以让
我将理论知识与实践相结合。我希望通过实际项目来加深对 Java 编程语言和相关技术的理解。于是我
便查找了一些资料，看了一些开源项目，进行了一些改进。\n\n回答2：（开源项目的解释）',
3 '回答2：（开源项目的解释）\n\n这个项目其实是在 github 上找到的一个开源项目，主要是可
以支持一些常规的聊天项目，我对于聊天如何实现的比较感兴趣。顺便也想锻炼一下自己工程代码能
力，在网上就找到了这个开源项目和项目的一些比较完善的文档和介绍，再加上找了一个业务场景：租
房。所以就确定了这个项目。\n\n回答3：（学校课设的解释）',
4 '回答3：（学校课设的解释）\n\n这是学校的课设题目，然后在课设项目的基础上，查找了一些
资料，进行了一些改进。\n\n这个项目为啥和上个(之前)同学的项目一样？\n\n回答1：（开源项目的
回答）'
5 ]
• 删除文档
使用 方法，删除传入索引列表对应的文档列表；若不传入索引列表，则认为全量删除。如下
delete
所示：
代码块
1 vector_store.delete(ids=ids[:3])
测试一下删除是否成功：
代码块
1 doc_3 = vector_store.get_by_ids(ids[:3])
2 print(f"{[doc.page_content for doc in doc_3]}")

最终输出： ，可以看到确实删除成功。
[]
7.3.2.2 向量搜索
如果我们传入一个查询，向量存储将嵌入该查询，在所有嵌入的文档中执行相似性搜索，并返回最相
似的文档。
这体现了两个重要的概念：首先，需要有一种方法来衡量查询与任何嵌入文档之间的相似性。其次，
需要有一种算法能够高效地在所有嵌入文档中执行这种相似性搜索。
对于【相似性】这一点，之前已经讲过，再来回顾下：
• 欧氏距离（Euclidean Distance）：就是我们高中几何学的两点之间的直线距离。距离越短，相
似度越高。
• 余弦相似度（Cosine Similarity）：它忽略向量的绝对长度（大小），只关注两个向量在方向上
的差异。在文本和语义的世界里，“方向”代表“含义”，而“长度”往往只代表“文本的长
度”或“词汇的多少”。换句话说，余弦相似度关注的是 你们是否指向同一个方向 / 你们是
“ ” “
否代表同一个含义
”
因此，在捕捉语义上的相似性上，余弦相似度是更常用的度量方式。
对于【相似性搜索】则可以通过向量存储提供的搜索方法实现。
7.3.2.2.1 相似性搜索
想要获取根据相似性搜索的结果，即嵌入单个查询，并查找相似的文档，并将它们作为文档列表返
回。这可以使用 方法来实现。说明： 是根据【余
similarity_search InMemoryVectorStore
弦相似度】来捕捉语义的。
代码如下：
代码块
1 search_docs = vector_store.similarity_search(query="数据库表怎么设计的？", k=2)
2 for doc in search_docs:
3 print("*" * 30)
4 print(doc.page_content)
方法参数解释如下：
• ：输入的查询str。
query
• ：要返回的文档数。默认为 4。
k
打印结果如下：

代1 码块******************************
2 提供两种方案有以下好处：
3
4 降低门槛：单机版让用戶快速体验核心功能，避免初期陷入复杂部署。
5
6 灵活演进：用戶可从单机版过渡到集群版，逐步适应生产需求。
7
8 业务专属问题
9
10 为什么将房源表拆分为水平分表（如按状态分表）？具体如何实现？
11
12 原因：
13
14 单表数据量过大（如房源状态频繁更新）会导致查询性能下降；
15
16 不同状态（上架/已租/下架）的查询频率不同，拆分可减少锁争用；
17 ******************************
18 数据库：MySQL + MyBatis
19
20 MySQL 和 MyBatis 是数据存储和访问的一个常见的做法。MySQL 是一个开源的关系数据库管理系
统，它可以免费使用。MyBatis 提供了一种相对简单的方法来执行 SQL 语句，不需要编写复杂的
ORM 映射文件。
21
22 缓存：Redis
23
可以看见，搜索出来的内容，是较为符合我们的预取。
有的同学可能有疑问了：搜索出来的结果，为什么不直接是问题的答案，而只是相似的文档呢？
那么恭喜你已经具备了构建智能应用基础逻辑了。此时我们可以将 找到的最相关内容 和我们 提
“ ” “
出的问题 一起交给LLM 来生成答案，这便能极大地提高答案的准确性和时效性。以上流程，叫做检
”
索增强生成（Retrieval-Augmented Generation, RAG），这是当前大语言模型应用的核心模式。
RAG 我们将会在后续内容进行教学。
7.3.2.2.2 元数据过滤
回顾一下，每个 对象，都包含了以下参数：
Document
• ：可选的文档标识符。理想情况下，这应该在整个文档集合中是唯一的，并格式化为
id
UUID，但不会强制执行。
• ：字符串文本
page_content
• ：与内容关联的任意元数据。类型为
metadata dict [Optional]

虽然向量数据库实现了搜索算法，来有效地搜索所有嵌入的文档以找到最相似的文档。但现实场景
中，我们还希望通过先根据元数据进行过滤，来帮助缩小搜索范围。例如从特定来源或日期范围检索
文档。
代码块
1 from langchain_core.documents import Document
2
3 def _filter_function(doc: Document) -> bool:
4 return doc.metadata.get("source") == "hahaha"
5
6 search_docs = vector_store.similarity_search(
7 query="数据库表怎么设计的？",
8 k=2,
9 filter=_filter_function
10 )
11 for doc in search_docs:
12 print("*" * 30)
13 print(doc.page_content)
这次，我们给搜索方法加入了 参数，它接收一个 bool 值，表示我们可以根据条件选择是否
filter
过滤某些文档。因此我们定义了一个 过滤函数，可以根据文档元数据先过滤出
_filter_function
文档，再去进行搜索。
示例中，我们给出的 是无效的，因此会将所有的文档都过滤掉，导致示例代码
source=="hahaha"
运行后结果为空！将 换成正确的来源后，再次运行则结果正确。
source
7.3.3 Redis 向量存储
我们还可以使用 Redis 来存储向量。大多数开发者都熟悉 Redis，因为它速度快、拥有庞大的客戶端库
生态系统，并且多年来已被众多大型企业采用。从本质上讲，Redis 是一种键值型的 NoSQL 数据库，
除了传统用例之外，Redis 还提供了诸如搜索和查询功能等额外能力，允许用戶在 Redis 内创建二级
索引结构。这使得 Redis 能够以缓存的速度充当向量数据库。
7.3.3.1 基本概念
理解 RediSearch
RediSearch 是 Redis 官方提供的一款高性能【搜索】与【全文索引】引擎模块。它基于 Redis 构建，
使用戶能够直接在 Redis 数据库中执行复杂的【搜索】和【分词查询】，无需额外引入外部搜索引
擎。RediSearch 特别适用于轻量级、响应速度要求较高的分词搜索场景。
RediSearch 提供了内置的分词功能，既避免 LIKE 的局限性（无法支持分词查询。例如：数据
为“Apple iPhone 17 Pro Max 256GB 星宇橙色”，搜“苹果17橙色”无法匹配），又无需部署 ES 这
种大型组件，集成简单、性能出色，非常适合中等规模的全文检索需求。

理解 Index
Index（索引）是 RediSearch 模块里的概念，用于定义一个查询目录。Index 是一个独立的数据结
构，它建立在多个 Redis Keys （Hash 类型）之上，这专门为了极速执行文本搜索、过滤和聚合而设
计。它本身不存储数据，而是存储了指向其他 Redis Keys 的指针，和这些 Keys 中特定字段的索引信
息。
理解 Index Fields
Index Fields（索引字段） 是创建索引时，明确指定的那些需要被索引的字段。它们定义了索引的“结
构”或“蓝图”，告诉 RediSearch：“请针对这些字段的内容，以其特定的方式为我构建快速搜索的
能力。”
可以把它想象成在一本书后面制作索引页（比如人名索引、主题索引）。我们不会把书中的每一个字
都做到索引里，而是只选择那些重要的关键词（字段），并记录下它们出现的页码（文档ID）。这里
的“关键词”就是 Index Fields。
在 RediSearch 中，索引字段是有特定的类型：
• ：精确匹配的分类/标签，可以用来多值分类、过滤、分组。
TAG
• ：整数/浮点数，可以进行数值范围查询，排序和统计。
NUMERIC
• ：全文搜索的字符串，支持分词、词干化、模糊匹配。
TEXT
• ：经纬度坐标，用来查询地理空间，计算距离。
GEO
理解 metadata schema
我们讲过，就是描述数据结构的声明格式。 则用来描述元数据的结构
schema metadata schema
声明。
这里的元数据是指我们将来要嵌入文档的元数据。因为对于文档元数据来说，它在存入 Redis 后，就
被定义成了索引字段。
对于文档元数据来说，里面存放的就是一些文档属性值，如 表示文档来源。我们还可以手动
source
加入其他元数据，这需要设置每个字段的声明： 表示字段名， 表示字段类型。
name type
代码块
1 metadata_schema=[
2 {"name": "category", "type": "tag"},
3 {"name": "num", "type": "numeric"},
4 ]
一张图总结关系，如下所示：

这里只展示了关系，实际的存储我们后续操作时会演示。
7.3.3.2 环境设置
使用 Redis 来存储向量，首先需要将相关环境配置好。
• 第一步：启动 Redis 服务端：使用 Docker 启动 Redis 实例。
这里要说明：对于  版本 ，使用：
Redis >= 8.0
代码块
1 docker run -d -p 6379:6379 -it redis:latest
对于  版本 ，使用：
Redis < 8.0

代1 码块docker run -d -p 6379:6379 redis/redis-stack:latest
使用 查看是否启动成功
docker ps
• 第二步：安装 Redis 客戶端包，以便将来定义客戶端，以及运行搜索和查询命令。
由于我们使用 Python 进行开发，则选择 库完成客戶端定义。要安装 redis-py，只需：
redis-py
代码块
1 pip install redis
• 第三步：在 LangChain 中想要使用 向量库，需要安装 包
Redis langchain-redis
代码块
1 pip install -qU langchain-redis
• 第四步：定义 Redis 连接 URL，客戶端连接 时需要使用。
Redis
Redis 连接 URL 的基本结构是：
代码块
1 [protocol]://[auth]@[host]:[port]/[database]
这部分根据自己Redis服务情况而定。例如：
"redis://localhost:6379"
• 第五步：测试连接（Ping）
代码块
1 import redis
2 redis_url = "redis://localhost:6379"
3 # 定义Redis客戶端
4 redis_client = redis.from_url(redis_url)
5 # Ping
6 print(redis_client.ping())
若输出 ，则表示连接测试成功。
True
7.3.3.3 基本操作

• 初始化
LangChain 中使用 初始化 向量存储。由于 Redis 需要相关配置，如连
RedisVectorStore Redis
接 URL 等，因此 LangChain 提供了 RedisConfig 配置类供我们使用。如下所示：
代码块
1 from langchain_openai import OpenAIEmbeddings
2 from langchain_redis import RedisConfig, RedisVectorStore
3
4 # 定义嵌入模型
5 embeddings = OpenAIEmbeddings(model="text-embedding-3-large")
6
7 # 配置 Redis 客戶端
8 redis_url = "redis://192.168.100.238:6379"
9 config = RedisConfig(
10 index_name="qa",
11 redis_url=redis_url,
12 metadata_schema=[
13 {"name": "category", "type": "tag"},
14 {"name": "num", "type": "numeric"},
15 ],
16 )
17 # Redis 存储初始化
18 vector_store = RedisVectorStore(embeddings, config=config)
 类，其初始化参数如下：
class langchain_redis.vectorstores.RedisVectorStore
• ：用于此存储的 Embeddings 实例。
embeddings
• ：可选的 RedisConfig 对象。
config
配置类，其关键配置参数如下：
class langchain_redis.config.RedisConfig
• ：Redis 中索引的名称。默认为生成的 ULID 唯一标识符。
index_name
• ：Redis Key 的前缀。如果未设置，则默认为 index_name。
key_prefix
• ：Redis 实例的 URL。默认为“redis://localhost:6379”。
redis_url
• ：元数据字段的 schema。设置该字段对于将来的元数据过滤有帮助。
metadata_schema
我们可以根据 查询其下的所有的 ，这需要安装  （
Index Name Index Fields redisvl pip
），使用   命令行工具来检查索引。
install -U redisvl rvl
代码块
1 rvl index info -i qa --host 192.168.100.238 --port 6379

可以看到 中就包括了：
Index Fields
• ：文档文本。
text
• ：向量，类型为数组。
embedding
• ：索引名。
_index_name
• ：文档元数据。
meteData schema
其中文档元数据的每个属性还都被当作单独的 。
Index Fields
• 添加文档
我们可以使用 方法，向向量库中去添加文档。这次我们可以给被分割的文档添加相
add_documents
关的元数据。如下所示：
代码块
1 from langchain_community.document_loaders import UnstructuredMarkdownLoader
2 from langchain_text_splitters import CharacterTextSplitter
3
4 # 生成分割器
5 text_splitter = CharacterTextSplitter.from_tiktoken_encoder(
6 encoding_name="cl100k_base", chunk_size=200, chunk_overlap=50
7 )
8 # 加载文档
9 data = UnstructuredMarkdownLoader("../Docs/Markdown/脚手架级微服务租房平台
Q&A.md", category="QA").load()
10 # 分割文档
11 documents = text_splitter.split_documents(data)
12 # 为文档添加元数据
13 for i, doc in enumerate(documents, start=1):
14 doc.metadata["category"] = "QA"
15 doc.metadata["num"] = i
16
17 ids = vector_store.add_documents(documents=documents)
18 print(f"共编排了{len(ids)}个文档索引")
19 print(f"前3个文档的索引是：{ids[:3]}")

结果输出：
代码块
1 共编排了96个文档索引
2 前3个文档的索引是：['qa::01K4Q0A3DSQVZBRFKJD5MS25HG',
'qa::01K4Q0A3DSQVZBRFKJD5MS25HH', 'qa::01K4Q0A3DSQVZBRFKJD5MS25HJ']
在 Redis 中的存储如下所示，下面的 字段被转换为二进制存储，因此展示了乱码。除此
embedding
之外，还存放了我们给文档设置的元数据信息。
• 获取文档
使用 方法，通过索引列表获取对应的文档列表。如下所示：
get_by_ids
代码块
1 ids = [":01K4Q0A3DSQVZBRFKJD5MS25HJ", ":01K4Q0A3DSQVZBRFKJD5MS25HK",
":01K4Q0A3DSQVZBRFKJD5MS25HM"]
2 doc_3 = vector_store.get_by_ids(ids)
3 print(f"{[doc.page_content for doc in doc_3]}")
结果如下：
代码块
1 [
2 '回答3：（学校课设的解释）\n\n这是学校的课设题目，然后在课设项目的基础上，查找了一些
资料，进行了一些改进。\n\n这个项目为啥和上个(之前)同学的项目一样？\n\n回答1：（开源项目的
回答）',
3 '这个项目为啥和上个(之前)同学的项目一样？\n\n回答1：（开源项目的回答）\n\n因为这个项
目本身就是开源项目，网上一搜都是好多都是这个项目的博客分析，写得优质的主要就是那几篇，可能
我们参考了一样的博客讲解梳理框架吧。抽奖系统虽然听起来简单，但实际上涉及到数据存储、状态转
换、异常处理等多个技术点。\n\n回答2：（学校课设的回答）',
4 '回答2：（学校课设的回答）\n\n这个问题面试官可能会直接的指出：上个同学和你学校不同，
但是跟你的项目完全一样。\n\n这个项目确实是我们学校的开源项目，但是我在实现的时候也在网上找

了跟课设要求类似的开源项目，而且发现网上针对这个类型的项目文档还听完善的，可能其他的同学也
是在网上跟我一样找到了类似的博客或开源项目进行借鉴的吧。'
5 ]
• 删除文档
使用 方法，删除传入索引列表对应的文档列表。如下所示：
delete
代码块
1 vector_store.delete([":01K4Q0A3DSQVZBRFKJD5MS25HJ"])
或者：
代码块
1 # 删除指定内容
2 vector_store.index.drop_keys(["qa::01K4Q0A3DSQVZBRFKJD5MS25HJ"])
3
4 # 全量删除，删除索引
5 vector_store.index.delete(drop=True)
7.3.3.4 向量搜索
7.3.3.4.1 相似性搜索
想要获取根据相似性搜索的结果，即嵌入单个查询，并查找相似的文档，并将它们作为文档列表返
回。这可以使用 方法来实现。代码如下：
similarity_search
代码块
1 search_docs = vector_store.similarity_search(query="数据库表怎么设计的？", k=2)
2 for doc in search_docs:
3 print("*" * 30)
4 print(doc.page_content)
方法参数解释如下：
• ：输入的查询str。
query
• ：要返回的文档数。默认为 4。
k
打印结果如下：

代1 码块******************************
2 提供两种方案有以下好处：
3
4 降低门槛：单机版让用戶快速体验核心功能，避免初期陷入复杂部署。
5
6 灵活演进：用戶可从单机版过渡到集群版，逐步适应生产需求。
7
8 业务专属问题
9
10 为什么将房源表拆分为水平分表（如按状态分表）？具体如何实现？
11
12 原因：
13
14 单表数据量过大（如房源状态频繁更新）会导致查询性能下降；
15
16 不同状态（上架/已租/下架）的查询频率不同，拆分可减少锁争用；
17 ******************************
18 数据库：MySQL + MyBatis
19
20 MySQL 和 MyBatis 是数据存储和访问的一个常见的做法。MySQL 是一个开源的关系数据库管理系
统，它可以免费使用。MyBatis 提供了一种相对简单的方法来执行 SQL 语句，不需要编写复杂的
ORM 映射文件。
21
22 缓存：Redis
除了上面的 方法，其实还提供了：
similarity_search
• 根据向量搜索方法：
similarity_search_by_vector
• 根据查询搜索方法，并返回相似分值：
similarity_search_with_score
• 根据向量搜索方法，并返回相似分值：
similarity_search_with_score_by_vector
例如，我们希望【根据查询语句搜索，并返回相似分值】：
代码块
1 scored_results = vector_store.similarity_search_with_score(query="数据库表怎么设
计的？", k=4)
2 for doc, score in scored_results:
3 print("*" * 30)
4 print(f"Content: {doc.page_content[:100]}...")
5 print(f"Metadata: {doc.metadata}")
6 print(f"Score: {score}")
结果如下：

代1 码块******************************
2 Content: 提供两种方案有以下好处：
3
4 降低门槛：单机版让用戶快速体验核心功能，避免初期陷入复杂部署。
5
6 灵活演进：用戶可从单机版过渡到集群版，逐步适应生产需求。
7
8 业务专属问题
9
10 为什么将房源表拆分为水平分表（...
11 Metadata: {'source': '../Docs/Markdown/脚手架级微服务租房平台Q&A.md', 'category':
'QA', 'num': 79}
12 Score: 0.580324351788
13 ******************************
14 Content: 数据库：MySQL + MyBatis
15
16 MySQL 和 MyBatis 是数据存储和访问的一个常见的做法。MySQL 是一个开源的关系数据库管理系
统，它可以免费使用。MyBatis 提供了一种相对简...
17 Metadata: {'source': '../Docs/Markdown/脚手架级微服务租房平台Q&A.md', 'category':
'QA', 'num': 23}
18 Score: 0.582783222198
注意：分数越低表示相似度越高。
7.3.3.4.2 元数据过滤
对于上述列举的方法，在他们搜索之前，都可以根据元数据先进行过滤。对于
RedisVectorStore
来说，需要使用 Redis 过滤表达式进行筛选。示例如下：
代码块
1 from redisvl.query.filter import Tag
2 # 过滤表达式
3 filter_condition = Tag("source") == "hahahaha"
4 scored_results = vector_store.similarity_search_with_score(
5 query="数据库表怎么设计的？",
6 k=2,
7 filter=filter_condition
8 )
9 for doc, score in scored_results:
10 print("*" * 30)
11 print(f"Content: {doc.page_content[:100]}...")
12 print(f"Metadata: {doc.metadata}")
13 print(f"Score: {score}")

示例中，我们给出的 是无效的，因此会将所有的文档都过滤掉，导致示例
source=="hahahaha"
代码运行后结果为空！将 换成正确的来源后，再次运行则结果正确。
source
对于 Redis 过滤表达式，也可以使用 和 运算符组合。如我们现根据元数据中的 与
& | category
进行过滤，如下所示：
num
代码块
1 from redisvl.query.filter import Tag, Num
2 # 过滤表达式
3 category_is_qa = Tag("category") == "qa"
4 num_is_under_50 = Num("num") < 50
5 filter_condition = category_is_qa & num_is_under_50
6
7 scored_results = vector_store.similarity_search_with_score(
8 query="数据库表怎么设计的？",
9 k=2,
10 filter=filter_condition
11 )
12 for doc, score in scored_results:
13 print("*" * 30)
14 print(f"Content: {doc.page_content[:100]}...")
15 print(f"Metadata: {doc.metadata}")
16 print(f"Score: {score}")
结果如下：
代码块
1 ******************************
2 Content: 数据库：MySQL + MyBatis
3
4 MySQL 和 MyBatis 是数据存储和访问的一个常见的做法。MySQL 是一个开源的关系数据库管理系
统，它可以免费使用。MyBatis 提供了一种相对简...
5 Metadata: {'source': '../Docs/Markdown/脚手架级微服务租房平台Q&A.md', 'category':
'QA', 'num': 23}
6 Score: 0.582783222198
7 ******************************
8 Content: 咨询消息的持久化优化
9
10 分库分表：按user_id哈希分库，按月份分表（如chat_msg_2023_10）；
11
12 读写分离：
13
14 写操作：主库（高并发下队列缓冲）；
15
16 读操作：从库 + Redis缓存最...

17 Metadata: {'source': '../Docs/Markdown/脚手架级微服务租房平台Q&A.md', 'category':
'QA', 'num': 42}
18 Score: 0.657752811909
7.3.3.4.3 最大边际相关性搜索
回顾一下什么是最大边际相关性？它是一种重新排序算法，它使用语义相似性作为基础工具，从一个
候选集中挑选出一组既能代表查询主题又彼此多样化的结果。
• 【语义相似性】就像面试官衡量每个应聘者与职位要求的匹配度。他会给每个应聘者打一个分数。
• 【最大边际相关性】就像团队经理（MMR算法）要组建一个团队。目标是选出一组“精华”结果，
而不是一个单一结果：
MMR 使用场景：
• 推荐系统：推荐与用戶兴趣相关但又不同类型的物品，避免“信息茧房”。
• 文档摘要：从长文档中选择能代表主旨又包含不同信息的句子，避免摘要内容重复。
• RAG (检索增强生成)：在从知识库检索完一堆相关文档后，使用 MMR 进行去重和多样化筛选，再
交给LLM生成答案，能有效提升答案质量和减少幻觉。
使用 MMR 搜索，需要用到 方法，代码如下：
max_marginal_relevance_search
代码块
1 from redisvl.query.filter import Tag, Num
2 # 过滤表达式

3 category_is_qa = Tag("category") == "qa"
4 num_is_under_50 = Num("num") < 50
5 filter_condition = category_is_qa & num_is_under_50
6
7 mmr_results = vector_store.max_marginal_relevance_search(
8 query="数据库表怎么设计的？",
9 k=2,
10 fetch_k=10,
11 filter=filter_condition
12 )
13 for doc in mmr_results:
14 print("*" * 30)
15 print(f"Content: {doc.page_content[:100]}...")
16 print(f"Metadata: {doc.metadata}")
我们需要注意一个参数 ，  是 MMR 算法第一步中，从向量库中初步获取的候选
fetch_k fetch_k
文档数量。
为了理解它，我们首先要明白 MMR 搜索是一个两阶段过程：
1. 初步获取：系统首先根据查询的纯向量相似度，从庞大的向量库中找出最相似的   个文
fetch_k
档。这一步的目标是“广撒网”，先找到一个足够大的相关文档池。
2. 重新排序与筛选：然后，MMR 算法会在这个较小的候选池（大小为  ）中运行。它不再
fetch_k
只考虑与查询的相似度，还会考虑候选文档之间的多样性。它会从这   个文档中，挑选
fetch_k
出既与查询相关，彼此之间又不太相似的   个文档作为最终结果。
k
因此，其目的就是在保证相关性的前提下，提升结果的多样性。打印结果如下：
代码块
1 ******************************
2 Content: 数据库：MySQL + MyBatis
3
4 MySQL 和 MyBatis 是数据存储和访问的一个常见的做法。MySQL 是一个开源的关系数据库管理系
统，它可以免费使用。MyBatis 提供了一种相对简...
5 Metadata: {'source': '../Docs/Markdown/脚手架级微服务租房平台Q&A.md', 'category':
'QA', 'num': 23}
6 ******************************
7 Content: 生产环境搭建
8
9 develop合并master
10
11 流水线部署
12
13 线上验证
14

15 你是如何优化项目性能的？你有哪些性能调优的经验？
16
17 房源搜索的筛选+排序用设计模式优化
18
19 热点数据存放缓存的优化
20
21 没有性能...
22 Metadata: {'source': '../Docs/Markdown/脚手架级微服务租房平台Q&A.md', 'category':
'QA', 'num': 40}
7.3.4 Pinecone 向量存储
7.3.4.1 Pinecone 介绍
Pinecone 是为机器学习应用量身打造的生产级向量数据库服务，适用于高维向量数据的高效存储、索
引与查询。它屏蔽了基础设施管理，提供无缝扩展、实时数据写入和强大安全保障，让开发者和数据
科学家能够以极低运维成本，快速构建高效的相似度搜索、推荐系统和 AI 应用。
Pinecone 是一个全托管的向量数据库平台，即负责所有后端维护、扩展、更新和监控，让用戶专注于
应用开发，无需担心数据库管理。
Pinecone 地址：https://www.pinecone.io/（魔法上网）
7.3.4.2 环境设置
• 首次使用需注册新用戶，选择个人免费版
• 继续创建账戶相关信息，或者直接右上角 skip 跳过。这里我们直接跳过。

• 注册成功会生成一个默认的 API Key。注意保存好你的 key。
• 也可以创建新的 API key：

• 设置 ，将 Key 添加进环境变量。
PINECONE_API_KEY
• 更新包
代码块
1 pip install -qU pinecone langchain-pinecone

7.3.4.3 基本操作
• 初始化
LangChain 中使用 类初始化 向量库。我们需要：
PineconeVectorStore Pinecone
1. 创建索引，参考这里。
2. 使用索引来初始化 。
PineconeVectorStore
如下所示：
代码块
1 from langchain_openai import OpenAIEmbeddings
2 from langchain_pinecone import PineconeVectorStore
3 from pinecone import Pinecone, ServerlessSpec
4
5 # 建立索引
6 pc = Pinecone()
7 index_name = "qa"
8 if not pc.has_index(index_name):
9 pc.create_index(
10 name=index_name, # 索引名称
11 dimension=3072, # 尺寸，表示向量维度，需要和嵌入模型维度一致
12 metric="cosine", # 度量方式，cosine 表示余弦相似度
13 spec=ServerlessSpec(
14 cloud="aws", # 亚马逊云
15 region="us-east-1" # 区域
16 ),
17 )
18
19 # 定义嵌入模型
20 embeddings = OpenAIEmbeddings(model="text-embedding-3-large")
21 # 获取索引
22 index = pc.Index(index_name)
23 # 定义 Pinecone 向量存储
24 vector_store = PineconeVectorStore(embedding=embeddings, index=index)
类，其初始化参数如
class langchain_redis.vectorstores.PineconeVectorStore
下：
• ：用于此存储的 Embeddings 实例。
embeddings
• ：Pinecone 索引
index
如果我们是第一次创建索引，在 Pinecone 控制台 则可以看见被创建的索引。

• 添加文档
我们可以使用 方法，向向量库中去添加文档。如下所示：
add_documents
代码块
1 from langchain_community.document_loaders import UnstructuredMarkdownLoader
2 from langchain_text_splitters import CharacterTextSplitter
3
4 # 生成分割器
5 text_splitter = CharacterTextSplitter.from_tiktoken_encoder(
6 encoding_name="cl100k_base", chunk_size=200, chunk_overlap=50
7 )
8 # 加载文档
9 data = UnstructuredMarkdownLoader("../Docs/Markdown/脚手架级微服务租房平台
Q&A.md", category="QA").load()
10 # 分割文档
11 documents = text_splitter.split_documents(data)
12 # 为文档添加元数据
13 for i, doc in enumerate(documents, start=1):
14 doc.metadata["category"] = "QA"
15 doc.metadata["num"] = i
16
17 ids = vector_store.add_documents(documents=documents)
18 print(f"共编排了{len(ids)}个文档索引")
19 print(f"前3个文档的索引是：{ids[:3]}")
结果如下：

1 共编排了96个文档索引
2 前3个文档的索引是：['eee6f5f6-696b-4088-94d0-d0511cc92623', '36b8f351-35e5-4096-
81ea-b29701a9b4c8', '8c133041-e2a4-4040-8b11-98b4d408a752']
控制台更新数据：
• 删除文档
代码块
1 # 全量删除
2 vector_store.delete(delete_all=True)
3
4 # 删除指定id的文档列表
5 delete_ids = []
6 vector_store.delete(ids=delete_ids)
7.3.4.4 向量搜索
想要获取根据相似性搜索的结果，即嵌入单个查询，并查找相似的文档，并将它们作为文档列表返
回。这可以使用 方法来实现。代码如下：
similarity_search
代码块
1 search_docs = vector_store.similarity_search(
2 query="数据库表怎么设计的？",
3 k=2,

4 filter={"category": "QA"},
5 )
6 for doc in search_docs:
7 print("*" * 30)
8 print(f"Content: {doc.page_content[:100]}...")
9 print(f"Metadata: {doc.metadata}")
结果打印如下：
代码块
1 ******************************
2 Content: 提供两种方案有以下好处：
3
4 降低门槛：单机版让用戶快速体验核心功能，避免初期陷入复杂部署。
5
6 灵活演进：用戶可从单机版过渡到集群版，逐步适应生产需求。
7
8 业务专属问题
9
10 为什么将房源表拆分为水平分表（...
11 Metadata: {'category': 'QA', 'num': 79.0, 'source': '../Docs/Markdown/脚手架级微
服务租房平台Q&A.md'}
12 ******************************
13 Content: 数据库：MySQL + MyBatis
14
15 MySQL 和 MyBatis 是数据存储和访问的一个常见的做法。MySQL 是一个开源的关系数据库管理系
统，它可以免费使用。MyBatis 提供了一种相对简...
16 Metadata: {'category': 'QA', 'num': 23.0, 'source': '../Docs/Markdown/脚手架级微
服务租房平台Q&A.md'}
8. 检索器（Retrievers）
8.1 相关概念
8.1.1 检索系统
检索系统（Information Retrieval System, IR System）是一个为了满足用戶信息需求，从大规模、非
结构化的数据集合中，自动、高效地查找、排序并返回相关信息的计算机系统。
它的核心任务是：在正确的时间，以正确的方式，将正确的信息传递给正确的人。最常见的例子就
是：搜索引擎（如Google、百度）。

随着大型语言模型的流行，检索系统已成为人工智能应用（例如 RAG）的重要组成部分。且存在多种
【不同类型】的检索系统，包括：
• 关系数据库
关系数据库是许多应用程序中使用的结构化数据存储的基本类型。数据存储在行（记录）和列（属
性）中，可以通过 SQL（结构化查询语言）进行高效的查询和作。关系数据库擅长维护数据完整性、
支持复杂查询以及处理不同数据实体之间的关系。
• 词法搜索索引
许多搜索引擎基于将查询中的单词与每个文档中的单词进行匹配，这种方法称为词法检索。即一个单
词经常出现在用戶的查询和特定文档中，那么这个文档可能是一个很好的匹配。这通常使用【倒排索
引】实现。
• 向量数据库
向量存储不使用字频，而是使用【嵌入模型】将文档转换为高维向量表示。这允许使用余弦相似度等
数学运算对嵌入向量进行有效的相似性搜索。
8.1.2 检索器
检索器是检索系统中的一个核心组件，它接收来自用戶接口的查询（Query），检索出包含查询关键词
的候选文档集合。
我们可以使用上面提到的任何检索系统实现方式创建检索器！如关系数据库、向量数据库等。由于其
重要性和多样性，LangChain 提供了一个统一的接口来与不同类型的检索系统进行交互。LangChain
的检索器接口非常简单：
1. 输入：查询字符串
2. 输出：文档列表（标准化的 LangChain 文档对象 Document）

例如，使用关系数据库的检索系统，检索器可以将问题转换为 SQL 语句，并执行查询，最后将查询结
果响应用戶。（该示例将来会在 LangGraph 中讲解，在这里帮助理解概念即可）
下面，我们来演示如何使用向量存储作为检索器。
8.2 使用向量数据库作为检索器
8.2.1 基本使用
向量存储是索引和检索非结构化数据的一种强大而有效的方法。可以通过调用向量数据库的
方法，将向量存储用作检索器。在这里我们使用 Redis 向量存储。
as_retriever
代码块
1 from langchain_openai import OpenAIEmbeddings
2 from langchain_redis import RedisConfig, RedisVectorStore
3
4 # 定义嵌入模型
5 embeddings = OpenAIEmbeddings(model="text-embedding-3-large")
6
7 # 配置 Redis 客戶端
8 redis_url = "redis://192.168.100.238:6379"
9 config = RedisConfig(
10 index_name="qa",
11 redis_url=redis_url,

12 metadata_schema=[
13 {"name": "category", "type": "tag"},
14 {"name": "num", "type": "numeric"},
15 ],
16 )
17 # Redis 存储初始化
18 vector_store = RedisVectorStore(embeddings, config=config)
19
20 retriever = vector_store.as_retriever()
21 docs = retriever.invoke("数据库表怎么设计的？")
22 for doc in docs:
23 print("*" * 30)
24 print(doc.page_content[:30])
LangChain 检索器是一个 Runnable 的对象 ，它是 LangChain 组件的标准接口。这意味着它有一些常
用方法，包括  ，用于与其交互。默认情况下，向量存储检索器使用相似性搜索。
invoke
输出结果如下：
代码块
1 ******************************
2 提供两种方案有以下好处：
3
4 降低门槛：单机版让用戶快速体验核
5 ******************************
6 数据库：MySQL + MyBatis
7
8 MySQL 和 M
9 ******************************
10 不同状态（上架/已租/下架）的查询频率不同，拆分可减少锁争用
11 ******************************
12 分库分表：按user_id哈希分库，按月份分表（如chat_
方法也支持我们传递相关参数修改搜索结果，如：
as_retriever
• ：设置相似算法，包括： （默
search_type “similarity”
认）、 或 （相似性分数阈值）
“mmr” “similarity_score_threshold”
• ：
search_kwargs
◦ ：限制检索器返回的文档   数量。
k k
代码块
1 retriever = vector_store.as_retriever(search_kwargs={"k": 2})

◦ ：要传递给 MMR 算法的文档量。
fetch_k
代码块
1 retriever = vector_store.as_retriever(
2 search_type="mmr",
3 search_kwargs={
4 "k": 2,
5 "fetch_k": 10
6 }
7 )
要注意的是， 检索器虽然是 Runnable 对象，但其不提供任何流式处理，因为它本身
Retrievers
通常是同步的、阻塞的操作。如下所示：
代码块
1 chunks = []
2 for chunk in retriever.stream("数据库表怎么设计的？"):
3 chunks.append(chunk)
4 print(chunk, end="|", flush=True)
调用可以发现，它是一次性返回，与 调用无异。
invoke
8.2.2 使用 创建“检索器”
@chain
除了使用 方法，我们还可以自行创建一个“检索器”。回想一下检索器的特点：
as_retriever
1. LangChain 检索器是一个 Runnable 的对象
2. LangChain 检索器输入为查询字符串，输出为文档列表（标准化的 LangChain 文档对象
Document）
综上所述，我们可以：
代码块
1 from langchain_core.runnables import chain
2 from typing import List
3 from langchain_core.documents import Document
4
5 @chain
6 def retriever(query: str) -> List[Document]:
7 return vector_store.similarity_search(query, k=2)
8
9 docs = retriever.invoke("数据库表怎么设计的？")

10 for doc in docs:
11 print("*" * 30)
12 print(doc.page_content[:30])
上面定义了一个函数，使用 修饰，该修饰可以使其成为 Runnable 函数，且满足检索器输入
@chain
输出的要求。在函数中，我们依旧使用向量数据库的相似性搜索方法，这样灵活性也更高，想要进行
元数据筛选也更方便。
注意，这并不是真正的检索器，检索器是一个 Runnable 对象，而我们定义的只是一个函数，具备其
特点罢了。
8.3 RAG 案例
RAG 是当前大语言模型应用的核心模式。当用戶向 LLM 提问时，系统首先使用嵌入模型在知识库中进
行语义搜索，找到最相关的内容，然后将这些内容和问题一起交给 LLM 来生成答案。这极大地提高了
答案的准确性和时效性。
由于 LangChain 检索器是一个 Runnable 的对象，我们便可以方便的使用 链 完成相关的调用。下面
我们来完成一个最简单的 RAG 案例，将会完成：
1. 根据 Query 搜索最相关的 4 篇文档。
2. 将相关的文档转换为字符串，以便后续发送给聊天模型。
3. 将 Query 与文档字符串发送给聊天模型。
4. 聊天模型依据输出解析器格式输出内容。
代码块
1 from langchain_openai import OpenAIEmbeddings, ChatOpenAI
2 from langchain_redis import RedisConfig, RedisVectorStore
3 from langchain_core.output_parsers import StrOutputParser
4 from langchain_core.prompts import ChatPromptTemplate
5 from langchain_core.runnables import RunnablePassthrough
6
7 # 定义聊天模型
8 model = ChatOpenAI(model="gpt-4o-mini")
9 # 定义嵌入模型
10 embeddings = OpenAIEmbeddings(model="text-embedding-3-large")
11
12 # 配置 Redis 客戶端
13 redis_url = "redis://192.168.100.238:6379"
14 config = RedisConfig(
15 index_name="qa",
16 redis_url=redis_url,
17 metadata_schema=[

18 {"name": "category", "type": "tag"},
19 {"name": "num", "type": "numeric"},
20 ],
21 )
22 # 定义 Redis 向量存储
23 vector_store = RedisVectorStore(embeddings, config=config)
24 # 生成检索器
25 retriever = vector_store.as_retriever()
26
27 # 定义提示词模板
28 prompt = ChatPromptTemplate.from_messages(
29 [
30 (
31 "human",
32 """你是负责回答问题的助手。使用以下检索到的上下文片段来回答问题。如果你不知
道答案，就说你不知道。最多只用三句话，回答要简明扼要。
33 Question: {question}
34 Context: {context}
35 Answer:""",
36 ),
37 ]
38 )
39
40 # 将文档转换为字符串
41 def format_docs(docs):
42 return "\n\n".join(doc.page_content for doc in docs)
43
44 # 定义链
45 rag_chain = (
46 {"context": retriever | format_docs, "question": RunnablePassthrough()}
47 | prompt
48 | model
49 | StrOutputParser()
50 )
51
52 # 执行链，流式输出
53 for chunk in rag_chain.stream("数据库表怎么设计的？"):
54 print(chunk, end="|", flush=True)
上述代码中， 我们之前还没有见过，简单来说，
RunnablePassthrough
 是一个“伪”Runnable，它的主要作用是在链（Chain）中透明地传递
RunnablePassthrough
输入数据，而不做任何修改。
当我们需要将原始输入和另一个处理过程的输出一起传递给下一个步骤时，就需要
。就例如代码中，我们需要将【Query】与【通过检索出来的文档转换的
RunnablePassthrough
字符串】同时发送给提示词模板。

最终打印结果如下：
代码块
1 |数据库|表|设计|可以|通过|水平|分|表|来|优化|性能|，|特别|是|当|单|表|数据|量|过|大|
时|，可以|减少|锁|争|用|和|提升|查询|效率|。|实现|方式|是|将|状态|字段|独|立|存|储|
在|house|_status|表|中|，并|通过|house|_id|进行|关联|查询|。|使用|My|SQL|
和|My|Batis|进行|数据|存|储|与|访问|，同时|可以|利用|Redis|缓存|来|进一步|提高|性
能|。|||
上面讲过检索器不提供任何流式处理。但这里可以使用流式输出。我们可以把像 RAG 这样的链的执行
过程想象成两个阶段：
• 阶段一：准备阶段（同步、阻塞）：接收输入、检索文档、构建提示
• 阶段二：生成阶段（可流式）：调用LLM
流式输出的是最终答案，而非中间过程。
